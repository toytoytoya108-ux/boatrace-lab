# boatlab — 引き継ぎメモ（Claude Code 用）

競艇3連単の「研究・予想システム」。当てる予想を出すことではなく、過去データで検証しながら
予想モデルを作り・改善し続けることが目的。開発の経緯と設計は `docs/00`〜`docs/10` を参照。

## 絶対に守るルール（リーク防止）
- レース結果を予想生成に一切使わない。予想は結果取得より前に保存する。
- 過去の予想は書き換えない（`predictions` テーブルは追記専用。トリガで UPDATE/DELETE を拒否）。
- 特徴量は対象レースの前日までの実績だけを使う（`boatlab/features/asof.py` の as-of 原則）。
- バックテストの封印テスト期間はモデル版ごとに1回だけ評価する。
- 見送りレースも仮想採点して記録する。モデル・設定はバージョン管理する。
- 正直さを最優先。「当たるように見せる」ことはしない。効かなければ効かないと報告する。

## いまの状態（2026-09-04 時点）
- 本番: KAGOYA VPS で稼働（PWA + スケジューラ）。`deploy/`、手順は `docs/10`。更新は VPS で
  `sudo bash /opt/boatlab/deploy/update.sh`。
- 採用モデル: **Model 1.0**（active）。Model 1.1（3seedアンサンブル）は shadow 並走候補で未採用。
- 買い方: **絞り込み型**（role='focused'、`FocusedParams`、docs/04 §16）を15点固定と並行して毎日保存。PWA上部トグルで切替。
- 資金配分（15点固定側）: **確率比例1乗**（`settings_versions.extra.staking`）。均等と回収率は同等だが「的中しても
  赤字」の割合が下がるため採用。`boatlab/model/staking.py`。
- これまでの正直な結論: 現行モデルに市場優位はない（3連単 log-loss 市場3.70 vs モデル3.78）。
  控除率は実測25%。回収率100%超えの買い方は見つかっていない。詳細は `reports/backtest/`。

## 進行中の作業: fs2 特徴量の効果検証
- 新特徴量42個を7グループに分けて追加済み（`boatlab/features/build.py` の `FEATURE_GROUPS`）:
  entry(進入) / st(実ST) / kimarite(決まり手) / weather(場×コース×風) / form(直近フォーム) /
  exh_trust(展示信頼度) / parts(部品交換)。リーク検査済み。
- 検証: `scripts/run_fs2_experiments.py`。基準値→各グループ単独→効いたものの組合せ、を四半期WFで比較。
  1実験=1サブプロセス（6GBメモリ対策）、期ごとに `.ckpt.pkl` へチェックポイント（途中落ち再開可）。
- 結果は `reports/backtest/fs2_experiments.{csv,md}` に追記。判定は 3連単 log-loss が基準比 −0.001 以上。
- 結果（run_fs2_fast.py、単一分割）: form −0.0058 / exh_trust −0.0014 が有効、form+exh_trust=3.8090（base 3.8171）。他は誤差。
- Model 1.2（form+exh_trust）本検証済み（`scripts/run_model12_wf.py`、`reports/backtest/model12_{valid,test}.md`）:
  valid log-loss 3.8108→3.8039、封印テスト 3.7983→3.7894。精度は一貫して改善、回収率は横ばい
  （15点 78.9→79.1%、絞り込み型 81.4→80.9%）。**未採用・未並走**（ユーザー判断: 別の改善を先に）。
  probstore は `data/probstores/m12_{valid,test}.pkl`（サンドボックスのみ）。
- 市場一致フィルタ検証（`scripts/run_market_filter.py`、`reports/backtest/market_filter_test_lgb.md`、2026実オッズ）:
  P/Q上限・β縮約・1位一致ゲートは再現せず不採用。安定して効いたのは上限オッズ50→20倍と期待値1.0→0.8
  （的中13%→23%、回収率83%で同等）。設定タブで変更可能（コード変更不要）。実オッズ期間の現行設定は的中≈13%・回収率77〜82%。

## データの置き場所（重要）
- `data/`（lab.db 2.2GB、features 1.3GB、probstores 3.2GB、models）は **.gitignore でリポジトリに入らない**。
- 別マシンで動かすときは lab.db を作り直す必要がある: `lab init-db` → `lab ingest-history --from 2018-01-01
  --to <今日>`（数時間）。特徴量キャッシュは `build_entry_dataset` が初回に自動生成する（1〜2時間）。
- VPS には lab.db がある（構築時に生成済み）。fs2 実験用の probstores はクラウドのサンドボックスにのみ存在。

## よく使うコマンド
```
pip install -e .                         # 開発インストール
python -m pytest tests -q                # テスト（DBが要るものは data/lab.db が必要）
lab init-db                              # テーブル+トリガ作成
lab ingest-history --from 2018-01-01 --to 2026-08-30
lab train --version 1.2 --seeds 7,17,27 # モデル学習→candidate登録
lab predict --version 1.0 --stage final # 締切前レースに予想保存（追記専用）
lab score                               # 結果が出た予想を採点
python scripts/run_backtest_campaign.py # 検証キャンペーン一式
python scripts/run_fs2_experiments.py   # fs2 グループ効果検証（進行中）
```

## コード地図
- `boatlab/ingest/` 取得層（Open API v3 / turnmark / 公式odds3t、原本キャッシュ、レート制限）
- `boatlab/store/` SQLite スキーマ・追記専用トリガ・書き込み
- `boatlab/features/` as-of 特徴量（asof.py が要）、fs2 拡張、履歴読込
- `boatlab/model/` strength(LightGBM) / trifecta(PL) / calibration / market / selection(15点) / staking / pipeline
- `boatlab/backtest/` dataset / walkforward(ProbStore) / metrics
- `boatlab/ops/` daily(ingest/predict/score/train) / scheduler(日次ループ)
- `boatlab/api/app.py` FastAPI + PWA(`web/`)
- `scripts/` バックテスト・実験の実行スクリプト

## 優先指標の順（設計方針）
回収率 ＞ 損益 ＞ 的中率 ＞ 期待値 ＞ 最大DD ＞ 最大連敗 ＞ 穴回収率 ＞ 直近成績。
実戦投入条件の具体値は後で決める（設定で変更可）。自動購入はしない。最終判断はユーザー。

## 買い方の自動研究（2026-09-08 追加）
- `boatlab/research/strategy.py`: 固定グリッド36通り（期待値×オッズ上限×自信×点数）を、研究用データ
  `research_data/bt2026_top20.npz`（2026年実オッズ3.7万R、Model 1.0 の上位20点確率、git管理・3MB）と
  本番の確定予想（predictions.probs/odds_used × results）に当てて評価。探索(1〜5月)/確認(6〜8月)/実運用の3期間＋
  順送り検証（各月、その月より前の回収率95%区間下限が最大の候補を当てる）。出力 `data/research/strategy_research.json`。
- スケジューラ 02:45 に `research` ジョブ。API `/api/research`（GET）・`/api/research/run`（POST、別スレッド）。
  PWA モデルタブ「買い方の研究（自動）」。**設定は自動で変えない**（候補の印付けまで。採用はユーザー）。
- 実測（バックテストのみ）: 順送り 81.3%。グリッドは増やさない（多重検定対策）。

## 場（レース場）別の検証（2026-09-08、結論: 効果なし）
- `scripts/run_stadium_study.py` → `reports/backtest/stadium_study.{csv,md}`。2026年実オッズ・現在設定。
- 全期間（1〜8月）で回収率100%超は **びわこ 102%（n=257、95%区間 81〜124%）のみ**。区間下限が100%超の場は0。
- 探索(1〜5月)の場別順位は確認(6〜8月)に引き継がれない: 順位相関 ρ=−0.03（p=0.91）。
- 場ラベルを無作為化した対照でも「最高の場」は平均108%、100%超が平均2.2場出る（実データは111%・3場）＝区別できない。
- 探索上位3場だけ買う → 確認 88.7%（無作為3場の分布で上位21%相当。全場82.6%との差は誤差の範囲）。
- 場ごとに36候補から最適パラメータを選ぶ → 確認 80.1% で**全場一律より悪化**（場あたりのデータが少なく過学習）。
- 「荒れやすさ（平均払戻）」と回収率の相関は 0.25（n=24、有意でない）。場別の分割はしない方針。

## 券種別の再検証（2026-09-09、実払戻のみ。結論: どの券種も100%未満）
- `scripts/run_bettype_study.py` → `reports/backtest/bettype_study.{csv,md}`。3連単120通り確率を各券種に
  周辺化し、results.payouts（公式の確定配当）で採点。**オッズ推定を一切使わない**（09-03 の検証は推定オッズ）。
  探索 2024-01〜2025-06（80,628R）／封印テスト 2025-07〜2026-08（62,801R）。
- 確率上位k点・100円均等の封印テスト回収率: 複勝1点 94.8% / 単勝1点 92.7% / 2連複2点 85.9% /
  ワイド1点 85.3% / 3連単1点 82.0% / 2連単2点 82.0% / 3連複1点 81.0%。点数を増やすほど悪化。
- 自信上位1%のレースに絞ると 複勝 98.7%（区間97〜101%、629R）・単勝 97.9%（95〜101%）が最接近。100%超えなし。
- モデルは公式払戻率を上回る＝素の市場平均には勝っている（3連単で+7pt）が、控除率25%を埋める25ptには届かない。
- **訂正**: 競艇の払戻率は全券種一律75%（控除率25%）。競馬と違い単勝・複勝も80%ではない
  （実測 Σ(1/確定オッズ) の逆数＝単勝73.6%・3連単74.8%）。単複が有利に見えるのは控除率ではなく分散の小ささによる。

## 単勝プールの歪み（2026-09-09、**初めて100%を超えた**。未検証・要追加データ）
- `scripts/run_pool_arbitrage.py` → `reports/backtest/pool_arbitrage.md`（commit 370aca8〜）。2026年1〜8月・確定オッズ・実払戻。
- 「1着になる確率」の精度（対数損失・確認期間）: **3連単プール 1.1474 < モデル 1.1720 < 単勝プール 1.2405**。
  市場（3連単プール）はモデルより正確。歪んでいるのは**薄い単勝プール**の方（売上シェア約0.08%）。
- 買い方: 単勝20倍以下で「3連単オッズ由来の勝率 ≥ 単勝オッズ由来の勝率×2」の艇を100円。
  **8か月4,571本で回収率184.1%**、月別160〜205%で全月プラス、上位10本の払戻を除いても179.7%。モデルは不要。
- 資金規模の制約が本丸: 単勝プール32,000円想定で1点100円→178.6%、1,000円→139.0%、2,000円→114.4%、5,000円→79.6%。
  1点1,000〜2,000円が上限。買いは全レースの約11%（1日19本）。
- **未検証**: 使ったのは両プールとも確定オッズ。実際は締切前オッズしか使えない。次にやること＝
  締切前の単勝オッズ（と3連単オッズ）の定期取得を本番に追加し、締切前→確定の動きと歪みの残存を実測する。
- 自動購入はしない・最終判断はユーザー、の原則は変わらない。

## 単勝・複勝プールの歪み: 観測フェーズの実装（2026-09-09）
- 目的は「締切前オッズでも歪みが見えるか」の実測。**購入はしない・設定も自動で変えない**。
- 取得: 公式 `oddstf`（単勝＋複勝を1回で）を締切6〜12分前と2〜4分前の2回。3連単は従来どおり6〜12分前に1回。
  公式サイトの上限は3秒間隔・1日1,000回、レースは平均159・最大216なので合計480〜650回に収まる。
  設定タブで停止すると取得ごと行わない（`extra.poolgap.enabled`）。
- 判定ロジック `boatlab/research/poolgap.py`: 3連単オッズ由来の確率 ÷ そのプールのオッズ由来の確率 ≥ 閾値
  （単勝2.0・複勝1.5、オッズ1.1〜20倍）。複勝は下限側オッズを使うので判定は厳しめ。
- 記録は `pool_gap_picks`（**追記専用トリガ付き**）。採点は読み取り時に results.payouts と突き合わせる方式で、
  テーブルを書き換えない。API `GET /api/poolgap`、PWA モデルタブ上部に表示。
- **合否の基準**（先に決めた）: ①締切前オッズと確定オッズの差が±10%以内の割合が過半（n≥200）
  ②締切前オッズで選んだ候補の仮想回収率が120%以上かつ95%区間下限が100%超。外したら打ち切る。
- パーサ未検証: サンドボックスから boatrace.jp に到達できないため `parse_oddstf` は実ページで未確認。
  本番で `lab check-oddstf --stadium <場> --race <R>` を1回実行して確認すること。生HTMLは
  `data/raw/official_web/oddstf/` に残る。読めなければ空 dict を返して記録をスキップする（既存処理は無影響）。

## 画面で自己診断できるようにする（2026-09-11）
- 発端: レース一覧が「5/12R終了・次 12:44締切」で止まって見え、上流待ちなのか故障なのか画面から判別できなかった。
  実体は上流 today.json が `result` の枠だけ作って中身（着順・払戻）を入れていない状態で、こちらは
  `_add_result` の pending_ok 分岐で意図的にスキップしていた＝正常動作。翌朝06:10の turnmark 取込で埋まる。
- `GET /api/status`: 本日の結果（DB件数・結果待ち件数・**上流に中身のある結果が何件あるか**を突き合わせ、
  ok / upstream（上流待ち）/ ng（取り込み異常）/ unknown を判定）、公式オッズ件数（3連単・単勝・複勝）、
  プール差の記録件数、確定予想と推定オッズ混入件数、直近ジョブ、24時間の取得失敗。上流照会は120秒キャッシュ。
- PWA 設定タブ最上部に「システムの状態」。レース一覧は締切超過で結果の無いレースを**結果待ち**表示に
  （場カードに「結果待ち◯R」、レース行にピル）。`_freshness` は2時間以上前の未完了ジョブを running から
  stuck に分離（過去の異常終了がいつまでも「実行中」に見えるのを防ぐ）。
- `lab today-status`（CLI）でも同じ突き合わせができる。tests/test_status_api.py で4つの判定を固定。

## 締切前オッズの動き（2026-09-12、`lab odds-drift`）
- 実測（9/11、144R・16,212買い目、Σ(1/オッズ)は締切前1.333/確定1.337で健全）: 全体の中央値 +34.5%、
  ±10%以内は14.4%。帯別は 5〜10倍 −8.1%、10〜20倍 −8.7%、20〜50倍 −2.3%、50〜200倍 +22.4%、200倍〜 +71.1%。
- **含意**: 単勝プール（中央値−50%）ほどではないが3連単も安定ではない。**これまでの実オッズ検証は
  すべて確定オッズで選定しており、絞り込み型の83%も同じ先読みバイアスを含む**（買う帯は中央値−8.4%なので
  概ね0.92倍に割り引いて読む）。実測値は9/11以降の本番データで直接確認する。

## route A「市場と組み合わせる」の結論（2026-09-12、**打ち切り**）
- `scripts/run_market_combine.py` → `reports/backtest/market_combine.md`。Benter 1994 の形
  `p ∝ モデル^a × 市場^b` を探索1〜5月で最尤推定し、確認6〜8月で1回評価（36,944R、3連単確定オッズ）。
- 合成は市場を上回った: 3連単 対数損失 市場3.7077 → 合成3.7026（**−0.0051**、a=0.150 b=0.926）。
  1着確率でも 1.1565 → 1.1552（−0.0014）。ただし勝ったのは**僅差**。
- **ケリー基準で決着**: 全通り比例買いの期待対数成長 = 情報優位 + log(1−控除率) = +0.0051 − 0.2877 = **−0.2826**。
  必要な優位の **56分の1** しかない。
- 実測でも、確認期間 1,701,240 買い目のうち「合成確率÷市場確率 > 1.333（＝期待値1.0）」は **12件**だけ（最大1.384）。
  モデル単独なら26.8%が期待値1.0超えに見えるが実回収率69.8%＝winner's curse。合成が a≈0.15 まで縮めるのが正しい判断。
- **一般的な結論**: 「この買い方が駄目」ではなく「このモデルと市場の差では、どんな買い方でも駄目」。
  勝つにはモデルを市場より大きく正確にするしかなく、必要な差は fs2 全体の改善（0.008 nats）の数十倍。

## 回収率の式が閉じた（2026-09-12、`scripts/run_sweet_spot.py`）
- 全券種で「市場（3連単オッズ）がいちばん確からしいと見る1点」を買い、確信の高いレースに絞って実測。
- **元返し（払戻100円の下限）は天井ではなく補助金**だった。元返しの割合が高い券種ほど回収率が高い
  （複勝 元返し77.7%→99.1% ＞ ワイド22.9%→91.2% ＞ 2連複1.1%→86.0% ＞ 3連単0%→83.1%）。
  確率0.94の艇の理論払戻は約80円だが下限で100円もらえる＝25%を胴元が被っている。
- **回収率 = 的中率 × max(1.00, 0.75 ÷ 市場確率)**。
  下限が効く側では回収率＝的中率（<1、実測の最高は94.6%）なので**絶対に100%を超えない**。
  下限の外では 的中率÷市場確率 > 1.333 が必要で、合成モデルでも170万買い目中12件しか出ない。
- 複勝上位10%の99.1%は両領域の混合。**券種の選び方では壁を越えられない**ことが確定。

## 群衆の癖を直接測る（2026-09-12、`scripts/run_crowd_bias.py`、結論: 実在するが1.14で足りない）
- 同じ特徴量で条件付きロジットを2本: A＝実際の1着を当てる / B＝市場の1着確率を再現する。
  係数の差 β_B−β_A が群衆の癖。線形・十数パラメータなので過学習の余地が小さい。
- **艇番が最大のズレ**: 2〜6号艇の係数が実際より大きい＝1号艇を過小評価（6号艇+0.228が最大）。
  これが人気薄バイアスの正体で、複勝99.1%を支えているのもこれ。
- **過大評価**: 展示タイム +0.120、級別 +0.084（目に見える情報に反応しすぎる）。
  **過小評価**: 全国勝率 −0.068、展示ST −0.055、モーター2連率 −0.051（地味な数字を軽視）。
- ズレの方向に賭けると 実勝率÷市場確率 は 上位10%で1.072 → 1%で1.139 → 0.2%(n=70)で1.243 と
  単調に上がるが鈍化し、**必要な1.333に届かない**。艇番を除くと上位2%で1.135が限界。
- 線形モデルA単独の1着対数損失は 1.1981 で市場 1.1455 に負ける（総合点では勝てない）。

## 他競技（競輪・競馬）で回収率100%は超えられるか（2026-09-12 調査、`reports/research/other_sports.md`）

競艇で導いた式 `回収率 = 的中率 × max(1.00, 払戻率 ÷ 市場確率)` はそのまま他競技に使える。
違うのは控除率・市場の歪みの大きさ・キャリーオーバーの有無の3点だけ。

- **競輪は一律25%控除（券種差なし）＝競艇と完全に同条件。** 必要比 1.333。構造的な得はゼロ。
- **JRA/地方の単勝・複勝のみ控除20%** → 必要比 1.250。競艇比で必要な歪みが 6.3% 小さい。
  それ以外（馬単・3連複 1.333／3連単 1.379／WIN5 1.429）は競艇と同等か不利。
- 本ツールで実測した競艇の歪みは 頑健 1.14 / テール70本 1.24。1.250 にわずかに届かない。
  → 控除率の差だけでは足りず、競馬の歪みが競艇より大きいことが別途必要。
  北米30場の実測（Green et al.）では本命端でも控除17%に対し回収は約2ポイントのみで、楽観は禁物。
- **キャリーオーバーは控除率をマイナスにできる唯一の仕組み**（WIN5 / 競輪Dokanto! / LOTO。競艇には無い）。
  `還元率 = 払戻率 + CO額 ÷ 売上`。WIN5で100%超の条件は **CO ≥ 売上の30%** で、15年で3回のみ
  （実測期待値 1.039/1.052/1.058）。直近5回は 78〜87%。COが大きいほど売上も増えて薄まる。
  かつ全網羅はパリミュチュエル山分けで成立せず（WIN5全網羅1.89億円、Dokanto!7で9.56億円）。
  **期待値プラス ≠ 実行可能。**
- **実在証明はJRAにある**（裁判で購入額・払戻額が確定）:
  2015年最高裁（大阪）2005-09年 購入28.7億/払戻30.1億＝**104.4%**、ソフト網羅購入。
  2017年最高裁（北海道）2005-10年 毎年100%超、2009年は全3,453R中2,445R(70.8%)で購入。
  → 可能だが**エッジは4〜8%**、5〜6年、年数億円の回転が前提。「良い予想」ではなく「小エッジ×巨大回転」。
- **税**: 払戻は原則一時所得で外れ券は経費にならない。上記2件が雑所得と認められたのは
  継続的・網羅的・機械的という特殊な態様ゆえ。一時所得扱いなら総回収率104%でも税引後マイナスになりうる。

**結論**: 競技を変えること自体は救済策にならない。検証するなら JRA 単勝・複勝で、競艇と同じ手順
（探索/確認分離・ラベルシャッフル・事前登録基準）で `p/q` 上位テールが out-of-sample で 1.250 を
超えるかを測る。作り直しではなく測定の問題。

## 校正（キャリブレーション）で回収率100%を超えられるか（2026-09-12、結論: 超えられない）

**回収率は我々が名乗る確率に一切依存しない。** 決まるのは「どの買い目を買ったか」と「市場がいくら払うか」
の2つだけ。校正が回収率に効く経路は「選択が変わる」「賭け金が変わる」の2つしかない。

- **選択**: 選択の曲線（絞り込み度 × 回収率）は `reports/backtest/{sweet_spot,favorite_edge,crowd_bias}.md`
  で全券種・全絞り込み度について既に測ってある。校正は**この曲線上のどこに立つかを正しくするだけで、
  曲線そのものを持ち上げない**。曲線の最高点は **複勝・市場確信度上位10% の 99.1%**（n=1,435）。
- **賭け金**: 曲線上のすべての点が100%未満なので、ケリー基準の最適賭け金は0。配分では救えない。

### データを貯めることで何が決着するか（本ツールで計算）

| 買い方 | 現n | 回収率 | 1本SD | 現95%幅 | 本/日 | ±0.5%に必要なn / 年数 |
|---|---:|---:|---:|---:|---:|---|
| 複勝 上位10% | 1,435 | 99.1% | 0.255 | ±1.3pt | 15.6 | 2,597 / **0.5年** |
| 単勝 上位10% | 1,352 | 94.5% | 0.409 | ±2.2pt | 14.7 | 6,703 / 1.2年 |
| crowd_bias 0.2%尾 | 70 | 93.2% | 0.831 | ±19.5pt | 0.8 | 27,634 / **99.5年** |

- **決着するのは1つだけ**: 複勝上位10%の99.2%が真に99%台なのか、上限の100.4%側なのか。約0.5年で±0.5%になる。
- **1.243に見えた尾（crowd_bias 0.2%）は検証に99.5年**かかる。原理的に確認できない＝使えない。
- 仮に真値が100.5%だったとしても、15.6本/日 × 100円で **年2,847円**、1,000円でも年28,466円。
  かつ競艇の複勝プールは売上の約0.07%しかなく、増額すれば自分でオッズを潰す。**スケールしない。**

### 100%を超えるのに必要な「新情報」の量

無情報（1着6通り一様）の対数損失 ln6 = 1.7918、市場 1.1565 →
**市場が持っている情報量は 0.6353 nats。** 控除率25%のコストは -log(0.75) = 0.2877 nats。

→ **控除率を埋めるには、市場の全情報量の 45.3% に相当する私的情報が追加で必要**（3連単基準でも26.6%）。
我々のモデル＋市場の合成が市場を上回った分は 0.0051 nats ＝ 市場の情報量の 0.80%、必要量の **1.77%**。

校正はこの0.0051を1ミリも増やさない（校正は情報を追加しない変換）。**足りないのは情報であって校正ではない。**

### 「勝てるレースを選ぶ」という設計思想について
方針として正しく、既に実行済み。全レース購入の回収率75%（＝控除率そのまま）を、選択によって
**99.1%** まで持ち上げてある。残り0.9%は元返し（100円下限）による構造的な天井で、絞り込みを
強めると的中率は上がるが平均払戻が100円に張り付くため積が下がる（上位10%が頂点、上位1%で96.4%）。

## 目的1の総括: 市場オフセット型ロジットによる5族の検証（2026-09-12、`reports/research/goal1_summary.md`）

**測り方を統一した。** 市場確率 q をオフセットに置いた条件付きロジット
`score_ij = log q_ij + γ·z_ij`。γ=0 なら市場が既に持っている情報。市場を作り直す必要がなく
「市場を超えた分」だけを直接測れる。共通土台は `boatlab/research/market_offset.py`。
**以降の目的1の検証はすべてこの形に統一する。**

**プラセボは必ず帰無仮説「市場確率が真」から生成（y ~ Multinomial(q)）。**
1着をシャッフルする作りは誤り。艇番の周辺分布と選択集合の対応が壊れ、選ばれた艇の多くが
1号艇なので比が2.5前後まで跳ね上がる（`wind_forecast.md` §4 に実例）。

| 市場に足した情報 | 確認対数損失 | 市場との差 | p/q上位2% | 必要量比 |
|---|---:|---:|---:|---:|
| 市場のみ | 1.1455 | +0.0000 | 1.000 | — |
| 艇番バイアス | 1.1444 | −0.0010 | 0.935 | 0.35% |
| ＋公表モーター2連率 | 1.1443 | −0.0012 | 1.052 | 0.42% |
| ＋分離推定モーター効果 | 1.1453 | −0.0002 | 1.029 | 0.07% |
| ＋本番風の予測残差 | 1.1444 | −0.0011 | 1.009 | 0.38% |
| ＋今節成績 | 1.1445 | −0.0009 | 0.999 | 0.31% |

プラセボの上位2%比はいずれも0.99〜1.02。実測0.94〜1.05は**帰無と区別できない**。
必要量 0.2877 nats に対し最良で 0.42%。**5族が独立に 0.001 nats 前後へ収束した。**

**診断: 我々のデータはほぼ市場の情報の部分集合。** 公開出走表から計算できるものは全部値段に
入っている。モデルの作り方・目的関数・校正ではこの構造は動かない。動かせるのはデータだけ。

個別の打ち切り記録:
- **アイデアD（風）**: `preview` 気象は1つ前のレースの観測値（一致率99.9%）で、本番風との一致は
  47%のみ。本番風は締切前情報で予測できた（確認期間 RMSE 1.1135→1.0113、+9.2% OOS）が、
  市場を条件づけた後の価値は +0.0000 nats。**予測できることと値段に対して価値があることは別。**
- **アイデアE（モーター分離）**: 公表2連率を条件づけた上積みは無し（M3 1.1453 > M2 1.1443）。
  当初の実装は探索期間の残差から e_m を作りその同じ期間で縮小定数を選んでいた＝答えの漏れ。
  k=10が選ばれ確認期間で市場より悪化(1.1779)。交差適合で修正後は符号もcrowd_biasと整合。
- **アイデアH（今節成績）**: 符号は事前予想どおり市場の過剰反応（今節平均着順 −0.0471、
  1着率 −0.0326）だったが、上積みは −0.0001 nats。

**手持ちデータの外に残るのは展示航走の映像解析のみ**（ターンの安定性・艇の挙動・水面の乱れ）。
出走表に存在しない量で、専門家も定性的にしか扱っていない。ただし映像はDBに無く2コアVPSでは
常時処理が不可能で、標本検証止まり。成功見積もり: 手持ちデータ内ほぼゼロ、映像込みで数%。

## 全レース穴買い（オッズ上限なし）の実測（2026-09-12、`reports/backtest/longshot.md`）

本命端の鏡像。36,251R全数、選定にパラメータを使わないので探索/確認の分割は不要。
`歪みの比 = 回収率 ÷ 0.75 = 実的中率 ÷ 市場確率`（1.00 なら市場は正しい）。

**いちばん人気がない1点を全レース**: 単勝 **34.1%**（比0.739）／複勝 45.9%／2連単 19.5%（比0.542）／
2連複 30.4%／ワイド 45.3%／3連複 27.5%／3連単 **27.6%**（的中8本/36,251R、最高払戻197,220円）。

**単勝の人気順の曲線が教科書どおり単調**（favorite-longshot bias の最もきれいな実測）:
人気1番目 92.2%（比1.042）→ 2番目 79.7%（0.966）→ 3番目 75.6%（0.961）→ 4番目 67.2%（0.938）
→ 5番目 55.2%（0.900）→ 6番目 34.1%（0.739）。
3連単も人気1〜5番目で比 1.047〜1.085（**全研究中で最良の比 1.085 が人気2番目**）、ROI 77〜80%。

**「穴が過大評価なら逆で儲かるのでは」への答え: 逆を取る取引が存在しない。**
人気薄をk点外して残りを払戻均等で買うと 86.5%（外さない）→87.1→88.1→89.1→90.2→92.2%（人気1点）と
単調に上がるが、これは「本命に寄る」のと同じで単勝本命端に一致するだけ。100%超えはどこにも無い。
最不人気を空売りできれば100円につき約45円だが公営競技に売り側は無い。
そして穴の誤りがプール全体に対して持つ大きさは **0.65%**（1.59% × (75%−34.1%)）＝配っても+0.7pt。
**市場のいちばん大きな誤りは、いちばん小さな確率の上に乗っている。だから取り出せない。**

注意: この表は賭け金に3連単プールの確率、払戻に単勝の確定配当を使うのでプール間の値付け差
（単勝1.2405 / 3連単1.1474）が一部混ざる。その差は締切前には取れない（観測フェーズで中央値−50%）。

## 万舟券の発生と特徴（2026-09-12、`reports/backtest/manshu.md`）

万舟＝3連単配当1万円以上＝オッズ100倍＝市場確率 0.0075。
**市場が示す万舟確率 = 確定オッズ100倍以上の買い目の確率の合計** で直接計算できる。

**発生頻度**（36,251R・241日）: 万舟 5,982本＝**16.50%＝1日24.8本＝6レースに1回**。
5万円以上 709本（1日2.9本）／10万円以上 208本（0.9本）／20万円以上 28本（0.1本）。
最高 501,250円、平均 7,076円、中央値 2,490円。月別の変動は 15.6〜17.2% で安定。

**市場は万舟を17%過大評価している**: 実測 16.50% / 市場が示す値 19.23% → 比 **0.858**。
`longshot.md` の穴買い（単勝34.1%）と同じ向き。

**過大評価は「市場が堅いと見たレース」でいちばん大きい**（市場19.23%を10分割した校正表）:
市場12.6%→実測9.3%（比0.736）… 市場27.4%→実測27.5%（比1.006）。単調。
→ **群衆は「堅いレースにも万が一がある」を買いすぎている。** 荒れると見たレースの値付けは正確。
市場の最高1着確率が70%以上のレースでも万舟率は12.4%あるが、市場はそれを16.0%と見ている（比0.774）。

**万舟が出やすい条件は実在する**: 波5cm以上18.4%／風7m以上18.1%／展示で2人以上が前づけ19.5%／
節の初日〜2日目18.2%／G2 20.2%・SG 19.6%・一般16.2%／場は江戸川19.6%・三国18.9% ⇔ 福岡12.1%・尼崎14.1%。
**ただしどの区分でも「実測÷市場」は1.00未満で、市場はこれらを全部知っている。**

**市場を超えた予測力は +0.0023 nats**（市場が示す万舟確率をオフセットに置いたロジスティック回帰、
確認期間の二値対数損失 0.4441→0.4418）。これは本研究で測った中では最大だが、必要な 0.2877 nats の 0.8%。
分散（荒れ）の予測は平均（勝者）の予測より少しだけ簡単だが、分散に払う券種は存在しない。

**目的2への応用は不発**: 複勝（市場確信度上位10%、99.1%）に荒れ信号 γ·z での追加見送りを入れても
99.0〜99.3%で動かない。天井は元返し（100円下限）で決まっており荒れの読みでは動かせない。

**万舟の正体は決まり手が一言で表す: 逃げ 3.6% / 差し 27.0% / まくり 33.2% / まくり差し 36.5%。**
万舟とは「1号艇が逃げられなかったレース」。1号艇の1着率約55%を市場は正確に値付けしている。

## 2モード表示ツールの設計材料（2026-09-12、`reports/backtest/two_modes.md`）

**穴モードの最重要の設計判断: 「穴」＝最不人気ではない。** 3連単を人気帯で買った実測（全36,251R）:

| 買う帯 | 点数 | 的中率 | 平均払戻 | 最長連敗 | 回収率 |
|---|---:|---:|---:|---:|---:|
| 人気1〜5 | 5 | 37.4% | 1,042円 | 23R | 78.0% |
| 人気16〜30 | 15 | 16.5% | 6,519円 | 67R | 71.6% |
| **人気20〜40** | 21 | 16.2% | 8,916円 | 51R | **68.8%** |
| 人気30〜50 | 21 | 10.3% | 13,517円 | 79R | 66.3% |
| 人気50〜70 | 21 | 5.2% | 25,484円 | 143R | 62.7% |
| 人気110〜120 | 11 | 0.3% | 138,484円 | **2,315R** | **37.5%** |

→ **万舟級の配当を狙うなら人気20〜50帯。最不人気1点(27.6%)の2.4倍の回収率。**

**レース選択が穴モードでは効く**（的中率モードと違って）: 市場が示す万舟確率の高い順に絞ると
人気20〜40の回収率は 68.8% →（上位30%）74.9% →（上位10%）**80.5%**。
歪みの比で 0.917 → 1.073。選定がオッズの**水準**でなく**散らばりの形**なので、
`pool_arbitrage.md` で崩れた選定とは性質が違う（要・締切前オッズでの確認）。

**現実的な運用設定の実測**:

| モード | 設定 | 1日の投資 | 的中率 | 月あたり的中 | 最長連敗 | 回収率 | 月の期待損失 |
|---|---|---:|---:|---:|---:|---:|---:|
| 穴 | 1日5R × 人気20〜40（21点） | 10,500円 | 22.5% | 34本 | 30R | 79.8% | 約63,600円 |
| 穴 | 1日5R × 人気20〜30（11点） | 5,500円 | 13.2% | 20本 | 40R | 78.2% | 約36,000円 |
| 的中率 | 1日5R × 複勝1点 | 500円 | 95.8% | — | 2R | **98.9%** | 約165円 |
| 的中率 | 1日15R × 複勝1点 | 1,500円 | 93.7% | — | 3R | 98.0% | 約900円 |

**月次収支は全帯・全8か月がマイナス**（全レース購入時）。人気1〜5（回収率78%）ですら 0/8か月。
**穴モードの期待損失は的中率モードの約385倍**。ツールに載せる際は必ず両方の「月の期待損失」を併記する。

※ すべて確定オッズでの測定。締切前オッズでの再現性は9/11から貯めているデータで要確認。

## 条件ベース選定ルールの検証（2026-09-12、`reports/backtest/condition_rules.md`）

**「期待値の高いものを選ぶ」はモデルの期待値では実装しない。** `market_combine.md`: モデルが
期待値1.0超えと見た買い目の実回収率は69.8%。代わりに**市場のオッズだけで決まる箱ごとの実測比**を
較正表にした（縦＝市場が示す万舟確率5分割、横＝候補の市場確率10分割、探索期間で作成）。

**較正表（実勝率÷市場確率）の最大値は 1.152**（必要なのは1.333）。パターンは明快:
候補の市場確率が高いほど比が上がり（0.426→1.152）、**堅いレースほど穴側の比が低い**
（堅い0.426 ⇔ 荒れ0.771）。`manshu.md` の「群衆は堅いレースの万が一を買いすぎ」と同じ絵。

**結論: 較正期待値による候補選定は不発。** 上位k点（k=5/10/20）× 閾値（なし/0.72/0.75）の
9通りすべてで確認期間の回収率は **69.0〜72.1%**、プラセボ（帰無「市場確率が真」）は **75.4〜76.1%**。
**どの設定も帰無を下回る。** 閾値条件を足すとむしろ悪化（条件なし70.4% → 0.75以上69.0%）。

**効くのはレース側の条件だけ**（人気20〜40の21点固定、確認期間で再評価）:
絞らない 71.2%（プラセボ74.9%）→ 市場万舟確率 上位30% **77.7%**（74.8%）→ 上位10% **83.3%**（76.5%）。
上位10%は1日14.0R・29,377円/日・的中23.3%・最長連敗26R。**95%区間 75〜92% で下限が帰無に接する。**
→ **「でたらめに穴を買うよりわずかにマシ」が穴モードの上限。利益が出る設定は存在しない。**

**的中率モードの条件（複勝1点・市場の2着以内確率のみ、確認期間）**:
0.80→57.1R/日・96.6%／0.85→32.9R/日・97.1%／0.88→19.2R/日・98.2%／
**0.90→10.6R/日・99.3%（95%区間98〜101%、最長連敗2R）**／0.92→4.4R/日・97.9%。
→ 閾値 **0.90** が最良。ただし確定オッズ基準で、締切前は中央値で約6.14pt低いので要再較正。

## 3連単（堅い予想）の「当たったのにマイナス」対策（2026-09-12、`reports/backtest/no_hit_loss.md`）

的中でも赤字は 払戻 < 投資合計 のとき。堅いレースほど本命の配当が安いので、購入判定レースでは
**均等配分で60.5%、確率比例2乗でも57.7%が「当たったのに赤字」**（配分では解けない）。

**保証つき配分**で構造的に解く: 各点を `ceil(予算 ÷ オッズ)`（100円単位切り上げ）で買い、
Σ賭け金 ≤ 予算 が成り立つ点数まで**本線を後ろから削る**。すると
**どの点が当たっても払戻 ≥ 予算 ≥ 投資** が成り立つ。

2026年・実オッズ・信頼度0.70以上（購入判定）での実測:

| 方式 | 平均点数 | 1レース投資 | 的中率 | 的中でも赤字 | 回収率 |
|---|---:|---:|---:|---:|---:|
| 10点・100円均等 | 10.0 | 1,000円 | 70.0% | **60.5%** | 79.3% |
| 10点・払戻均等（3,000円） | 10.0 | 3,016円 | 70.0% | 22.4% | 81.0% |
| **保証つき・予算3,000円** | 8.8 | 2,841円 | 66.2% | **1.0%** | 81.4% |
| 保証つき・予算5,000円 | 9.3 | 4,596円 | 68.1% | 1.2% | 81.4% |

→ 的中率 70.0→66.2% と引き換えに、的中でも赤字 60.5%→1.0%（残りは返還・端数）。回収率は不変（配分は選定を変えない）。
**予算3,000円を既定にする（今の1レース予算と同じ）。**

注意: 100円単位の丸めのせいで予算1,000円では払戻均等が成立しない（実質均等になる）。最初の実装は
これで「的中でも赤字25%」が残り、原因に気づいて作り直した。払戻均等には予算2,000円以上が要る。
削る判断に確定オッズを使っているので締切前では点数が前後する（本命は締切にかけて人気が集まり
オッズが下がるため、締切前に成立しても確定で崩れることがある）。

## 3モード表示ツール（2026-09-12 実装、`boatlab/model/modes.py`、modes_version="modes1"）

画面のメインは **穴狙い（3連単）／堅い予想（3連単）／複勝・単勝** の3モード。旧の15点固定・絞り込み型は
「旧モード（比較用）」ボタンから見られ、記録はこれまでどおり裏で続く（300レース到達時のCSVもそのまま）。

| role | 選定 | 発火条件 | 買い目 |
|---|---|---|---|
| `ana` | 市場 | 市場が示す万舟確率 ≥ 0.2495（上位10%） | 3連単 人気20〜40番目 21点 × 100円 |
| `katai` | モデル本線の並び＋保証つき配分 | 信頼度 ≥ 0.70、かつ3点以上成立 | 各点 `ceil(3000÷オッズ)`、Σ ≤ 3,000円になるまで後ろから削る |
| `place` | 市場 | 複勝: 2着以内確率 ≥ 0.90／単勝: 1着確率 ≥ 0.7709 | 各1点 × 100円 |

- **`predictions` に role='ana'/'katai'/'place' として追記**（既存の追記専用トリガ・採点をそのまま使う）。
  確定予想（stage='final'、締切4〜10分前）と同時に `daily._record_modes` が書く。本体・絞り込み型・3モードで
  1レース5行。`flags.params` に使ったしきい値を毎回保存するので、設定変更の前後が記録で追える。
- **市場ベースの2モード（ana/place）は推定オッズでは絶対に動かさない**。公式3連単オッズが無いレースは
  `skip_reason='odds_estimated'` として記録される（黙って欠ける記録は無い）。
- 複勝・単勝の採点は `daily.score_place`（`results.payouts` の place/win）。3連単2モードは既存 `score_race`。
- API: `/api/modes`（3モードの今日＋累計＋実測値＋月の期待損失）、`/api/today?mode=ana|katai|place`。
  画面は `measuredCard` で **実測回収率・的中率・平均払戻・最長連敗・月の期待損失を常時表示**（ユーザー指定）。
- CLI: `lab modes`（今日の記録）、`lab modes --stadium N --race R`（最新の公式オッズで今の選定を計算、記録はしない）。
- **しきい値はすべて確定オッズで決めた値。** 締切前オッズでは市場の確信度が低めに出るので発火は少なめになる。
  記録が2週間ほど貯まったら締切前基準で再較正する（自動では変えない。settings_versions.extra.modes で人が変える）。
- 設計の根拠: `two_modes.md`（帯の選択）、`condition_rules.md`（EV選定は不発・レース条件のみ有効）、
  `no_hit_loss.md`（保証つき配分）、`manshu.md`（万舟確率）。

### 2026-09-12 夕方の修正
- **状態画面の「確定予想 異常: n件が推定オッズ」は誤表示だった。** `flags LIKE '%odds_estimated%'` は
  `"odds_estimated": false` にも一致するので全件が異常に見えた。`json_extract(flags,'$.odds_estimated')=1` に修正。
  実際は「3連単141件」取れており正常。
- 3モードとも**見送りでも「買うならこれ」を記録**する（旧モードと同じ扱い）。見送りレースも仮想採点され、
  条件の良し悪しを後から測れる。推定オッズのときだけ市場ベースの2モードは何も出さない。
- 複勝・単勝の combo は主キー衝突を避けて「複1」「単1」の形。`score_place` は数字だけ取り出す。

### 2026-09-12 夜: 公式3連単オッズの「1000倍以上」が読めていなかった（実オッズ未取得の真因）
- 公式ページは 1000倍以上のオッズを **小数点なしの整数**（"1274"）で表示する。パーサは `\d+\.\d+` しか
  オッズと認めておらず、それらが None になっていた。大村12Rでは120通り中37通り（本命が堅いレースほど多い）。
- 予想側は「数値が100通り以上」で実オッズ採用を決めるので、**堅いレースほど推定オッズに落ちていた**。
  9/11の確認（`check-odds3t`）はキー数しか見ておらず、値が数値かを確認していなかった＝私の確認不足。
- 修正: 2桁以上の整数もオッズとして受ける（1桁は艇番）。カンマも除去。`check-odds3t` は数値の数で判定。
- 診断コマンド: `lab why-estimated --stadium N --race R`（DBから照合をやり直してどの条件で落ちたか）、
  `lab dump-odds3t --stadium N --race R`（保存済み生HTMLを再パースし、読めないセルを生HTMLごと表示）。
  生HTMLは DB ではなく `data/raw/official_web/odds3t/YYYYMMDD/` にある。
- 本番の CLI は `sudo docker compose exec app lab …`。ホストに `lab` は無い。
- 影響: 9/11〜9/12 の確定予想のうち、1000倍超が21通り以上あるレースは推定オッズで作られている。
  記録は追記専用なので直さない（flags.odds_estimated=true で識別できる）。

## 作業前・出荷前チェックリスト（2026-09-12 の反省から。ユーザー方針「作業を始める前に準備を徹底する」）

今日は4回の更新依頼と3つの本番不具合を出した。すべて「手元で確かめられたのに確かめなかった」もの。

**出荷前に必ず**
1. **本番DBのコピーでAPIを全部叩く**（`cp data/lab.db` → TestClient で `/api/today` `/api/modes` `/api/status` …）。
   さらに**記録ゼロ・賭け金ゼロ・NaN** の状態を作って叩く（新機能は初日に必ずこの状態を通る）。
2. **画面を触ったら Playwright で 390px 幅の描画を見る**（`/tmp/claude-0/ui_check.py` の型）。
3. **CLIを触ったら手元で実行する**（`CliRunner` か `python -m boatlab.cli`）。エラーが出る前提で。
4. **パーサは「件数」でなく「値」を確認する**。キーが120あっても値が None なら読めていない。
   本番の生HTML（`data/raw/…`）を1つ取り寄せて再パースし、`lab dump-odds3t` で読めないセルを見る。
5. **本番で使うコマンドはそのまま貼れる形で渡す**（`sudo docker compose exec app lab …`）。
6. 更新依頼は**まとめて1回**。小さな修正を都度出さない。

**作業前に**
- 触る箇所の既存コードを読み切ってから書く（今日は `modeBar` の CSS・`__main__` の位置・`raw_files` の有無を読まずに書いた）。
- 「動いているはず」の前提を1つずつ本番の記録で確認する（`why-estimated` のような自己診断を先に作る）。

## 万舟の深掘り（2026-09-12、全期間 463,527R、`reports/research/manshu_deep.md`、特徴量 `manshu_features.parquet`）

- 万舟率は **年ごとに 16.5〜17.5% で安定**（79,546本）。9割は2〜6号艇が1着。1号艇が勝つと万舟は3.6%。
- **いちばん効くのは「1号艇の弱さ」と「実力の拮抗」**: 6艇の勝率のばらつき≤0.5で×1.21 ⇔ ≥1.5で×0.72、
  1号艇−他艇最高の勝率差 +1以上で×0.71 ⇔ −2以下で×1.11、1号艇の勝率順位4〜6位で×1.15。
  LightGBM の寄与も 勝率のばらつき18.5%・場14.8%・1号艇勝率順位8.2% の順。
- **番組の意図がそのまま出る**: 企画レース（モーニング一般・ツッキー・ガチ勝ち8・昼どきピンクル）は万舟率 10〜11%（×0.6）、
  予選ドリーム・戸田特賞・日替艇食戦は 21%（×1.2）。ナイター ×0.88。2〜4R ×1.08〜1.10、12R ×0.93。
- 場: 平和島×1.18・鳴門×1.15・戸田×1.13・江戸川×1.10 ⇔ 大村×0.85・徳山×0.84・尼崎×0.89。
- 展示: 1号艇の展示タイム5〜6位×1.12、展示ST0.30以上×1.13、展示で1コースを取れず×1.17、前づけ2人以上×1.10。
  展示タイムのばらつき・チルト・部品交換は効かない（×1.0）。
- 気象: 風9m以上×1.19、波7cm以上×1.12。天候・気温・月・曜日は効かない。
- **事前特徴だけのモデルは AUC 0.585 で弱い。** 上位10%の万舟率23.3%は市場の見立て23.7%と一致（実測÷市場0.983）。
  市場が示す万舟確率の上位10%は27.5%（1.006）。事前条件には市場を超える情報が無い。
- 荒れ条件（1号艇B級・勝率差負・展示T4位以下・展示ST4位以下・前づけ・波5cm・風6m）を数えて絞ると
  穴21点（人気20〜40）の回収率は 0個62.6% → 5個77.3% と単調に上がるが、市場万舟確率上位10%の83.3%に届かない。
- **実測÷市場 > 1.00 は、市場自身が大混戦と見ている極端な区分だけ**（1番人気20倍以上 1.117 n=368、
  エントロピー4.1以上 1.015、100倍未満が50通り以上 1.069 n=372）。
- 結論: 穴を狙う場面の選定は市場が示す万舟確率を第一にする。条件分析は「なぜ荒れるか」の理解に使う。

## 穴狙いの買い方・レース選びの通説検証（2026-09-12、`reports/research/ana_playbook.md`）

予想サイト7つから通説を集め、2026年 36,251R の確定オッズ＋公式払戻で総当たり。前半（1〜5月）／後半（6〜8月）で再現確認。

- **万舟のとき1号艇は 着外49.3%・2着19.6%・3着19.5%・1着11.5%**。「1号艇2着づけ」は万舟の2割しか拾えない。
- **買い方: 人気20〜40の21点が、通説の18構造すべてに前半・後半とも勝つ。** 4-全-全 77.2%、2〜4頭×5・6の2着 76.6%、
  出目買い 75.9%、1号艇2着づけ 74.1%、1号艇消し 70.0%、3連複ボックス 68〜71%、ボックス 66〜71%（いずれも上位10%レース内）。
  ボックス2,4,6 だけ両半で上回る（83.3%）が的中3.2%・区間63〜108%・仕組み無し → 採用しない。
- **レース選び: 通説の条件を単独で使うとどれも実測÷市場 < 1（67〜75%）。** 女子戦・ルーキー戦は通説と逆で堅い
  （万舟率15.2%、回収率66.8%）。前づけ 69.9%、4カド単独 70.6%。市場万舟確率上位10%（1.006、80.5%）だけが超える。
- **現行に重ねて前半・後半とも上回った3条件**: ＋1号艇B級（82.3%、10R/日）、＋4カド条件（87.6%、3.9R/日）、
  ＋1号艇展示T4位以下（81.8%、6.2R/日）。7つ試して3つ通過＝帰無でも1.75個通るので、選定には入れない。
  **事前登録して前向きに検証**: 発火300R以上でタグ付き回収率 > タグ無し かつ差5pt以上、を合格基準とする。
- ツール反映（確定）: 選定・買い目は変えず、穴モードに**荒れ理由タグ**（1号艇B級／4カド／展示T4位以下／荒れる場／前づけ／
  1番人気12倍以上）と**堅い注意タグ**（女子・ルーキー・企画レース）を記録・表示。`lab modes` にタグ別集計。

## 画面の作り直しと荒れ理由タグ（2026-09-12 夜、SW bl-v16）

**荒れ理由タグ**（`modes.race_tags`、`flags.tags` / `flags.solid_tags` に3モードとも記録）:
l1_b・kado・l1_ext4（★事前登録）・rough_stadium・maezuke・top1_12。堅い注意: women_rookie・kikaku。
**選定には使わない。** 表示（レース行・詳細）と前向き検証（`/api/modes` の `by_tag`、`lab modes` のタグ別集計）のため。
合格基準（事前登録）: 発火300R以上で タグ付き回収率 > タグ無し かつ 差5pt以上。

**画面**（`web/index.html` を書き直し）: 一目で見たい情報を最優先。
- ホーム: モード1行 → **今日の買い目**（締切順、あと何分、点数・金額、タグ、結果）→ 今日の収支 → 実測 vs 記録 → タグ検証（穴）。
- レース: 場一覧 → 場ごとの一覧（全/推奨/見送り）。信頼度・EVのスライダは廃止。
- レース詳細: 結果 → 推奨/見送り → 買い目（フォーメーション or 複勝単勝）→ このレースの特徴（タグ）→ 他のモード → 6艇の評価（折りたたみ）。
  120通り評価・根拠・暫定予想は画面から外した。
- 成績: 累計KPI・実測 vs 記録・タグ検証・月別・場別。校正・信頼度帯・本線/穴・モデル版の内訳は外した。
- 設定: システムの状態（プール差の行は外した）・3モードの条件（`extra.modes` を versioned settings で保存）・CSV書き出し（3モード＋旧2モード）。
- **画面から外したがAPI・記録は残っているもの**: 旧15点固定/絞り込み型の表示と設定、単勝複勝プールの歪み（/api/poolgap）、
  買い方の自動研究（/api/research）、モデル版・バックテスト（/api/models, /api/backtests）、実戦投入判定（/api/readiness）、
  校正（/api/stats/calibration）。ナビの「モデル」タブは削除。
- 出荷前チェック: 59 tests、本番DBコピーで全API 200、Playwright 390px で5画面（横スクロールなし・JSエラーなし）、CLI を CliRunner で実行。

## 直前版の記録（2026-09-13〜、`daily.record_late_modes`、`lab late-drift`）

- 締切2〜4分前に3連単オッズをもう一度取り（1日約160回追加、上限1,000回の内）、穴・複勝単勝を
  **role='ana_late'/'place_late'** として追記する。`flags.late=True`、`flags.minutes_before`、`flags.overlap_with_early`
  （8分前の帯との重なり本数）、`flags.early_decision`。採点は flags.mode（'ana'/'place'）で既存経路。
- **表示には使わない。** 目的は「8分前の帯 → 3分前の帯 → 確定の帯」の入れ替わりと、直前版の仮想回収率の測定。
  穴モードの実測83.3%は確定オッズの数字なので、取得が確定に近いほど近づくはず。一方で21点の入力時間が要る。
  1週間ほど貯めて `lab late-drift` の数字で予想の時刻を動かすかを決める。
- 現行の確定予想が締切4〜10分前なのは旧絞り込み型（最大5点）の入力時間として決めた名残。取得回数の制限は理由ではない。
- 以前の「直前の変動」の数字は3連単の帯のものではない: 中央値−50%は単勝複勝の極端な1点の選定、約6ptは複勝の確信度。

## 4つ目のモード: 3連単（堅い予想・上位厚め）role=`katai_t`（2026-09-13、SW bl-v17）

- 選定は堅い予想と同じ（本線＝モデル確率順10点、信頼度0.70以上）。配分だけ **順位に反比例・合計3,000円固定**
  （`top_heavy_stakes(10,3000)` = 1000/500/300/300/200/200/200/100/100/100）。オッズは選定・配分に使わないので
  推定オッズのレースでも発火する。
- 実測（2026年実オッズ・購入判定レース、`no_hit_loss.md` P）: 回収率 **83.6%**（80〜87）、的中70.0%、
  **的中でも赤字46.9%**、損益は保証つきより約7万円良い。保証つき（81.4%・赤字1.0%）と両立しない性質なので並行記録する。
- 画面: ヘッダ4ボタン（穴狙い／堅い予想 保証つき／堅い·厚め 10点固定／複勝·単勝）。390px幅で収まることを確認。
- `Scoring.hit_kind` / `PredictionSelection.kind` は String(8) なので role 名は8文字以内（katai_t）。

## 穴狙いの「軸艇」構造の検証（2026-09-13、`reports/research/ana_axis.md` / `ana_axis2.md`、結論: 不採用）

ユーザー提案「荒れると市場が見る以上、中心にいる舟があるはず。それを軸に10〜15点」を、確定3連単オッズだけで決めた
5通りの軸（1号艇以外の最有力／100倍以上の目に最も多く現れる艇／同・1着に最も多い艇／本命／1着2番目）× 12構造 = 60通りで検証。

- **「荒れの中心の舟」はオッズから1艇に決まらない。** 上位10%レースで100倍以上の目に最も多く現れる艇は 4号艇31%・5号艇24%・
  3号艇16% に散らばり、その艇の1着率 9.4%・3着以内率 39.5%。荒れ方は2〜6号艇に分散している。
- **60通りすべてが、前半・後半とも現行の人気20〜40（80.5%）に勝てない。** 荒れの中心艇を軸にした構造は 68〜78%。
  両半で上回ったのは「本命1着 ∩ 人気20〜40」の2通りだけで、帰無シミュレーション（市場が正しい世界、20回）の平均9.9通りより少ない。
  軸を1艇に固定すること自体が、市場の人気順が持つレース固有の情報を捨てる。
- 唯一の手がかり「本命（多くは1号艇）が1着で2・3着が荒れる目」（crowd_bias の向きと一致）は、探索1〜5月→確認6〜8月の事前登録手順で
  **確認 83.9%（1号艇1着固定 × 人気10〜40、9.1点）vs 現行 84.8%** で超えず。帰無20回で83.9%以上は1/20だが、現行との差は無い。
- **ツールには反映しない。** 次に穴の買い方を動かすなら、締切前オッズでの現行の実測（`lab modes` 累計）を先に見る。

## 地方競馬（NAR）の単勝・複勝を測る（2026-09-13、`reports/research/nar_market.md`、探索段階・2026年3月のみ）

事前登録は `reports/research/nar_plan.md`（データを見る前に作成）。データは地方競馬情報サイトの公式CSV
（月別開催日程 → レース情報 / オッズ情報 ZIP）。**自動取得はしない**（robots.txt がダウンロード画面を除外、
規約も無断複製を禁止）。人が画面から落としたZIPを `data/nar/` に置き、`boatlab/research/nar.py` が読む。
オッズは **2026年3月以降しか無い**（レース情報は1998年から）。

- **データ検証**: 勝ち馬の `単勝オッズ×100` と公式の単勝払戻金が **99.5%完全一致**（残りは同着）＝確定オッズ。
  **取消の扱いが罠**: 出走取消・競走除外はオッズ行が無い（返還）が、**競走中止・失格はオッズも人気もあり外れになる**。
  着順の無い馬を全部落とすと1番人気が消えて払戻率が124%に見えるレースが出る（実際に一度踏んだ）。
- **実測払戻率 78.6%**（公表80%）。オッズの0.1刻み切り捨てで1.4pt増える。**必要比は1.250でなく1.272**。
  競艇の単勝（同じ測り方で73.6%＝1.359）より **6.8%有利**。13場すべて78.4〜79.0%で一様。
- **本命側の歪みは実在**: 1番人気 勝率44.5% vs 市場確率43.0%（比1.034）、オッズ1.0〜1.5倍帯で **比1.147**。
  市場確信度の上位で絞ると 単勝 81.7→89.2（上位10%）→**95.0%**（上位5%）、複勝 89.2→96.6→**98.6%**。
  帰無200回で上位10%・5%とも **0/200**。3月の前半・後半でも同じ向き。
- **それでも100%を超えない。理由は控除率でなく元返し**: 上位5%の1番人気は確定オッズ **1.0〜1.1倍**、
  複勝払戻は56/66が**ちょうど100円**。この領域では `回収率 ≒ 的中率`。上位5%の複勝的中率97.0%・単勝89.4%。
- **逆説**: 控除率が5pt低いのに、本命端の複勝は **競艇99.1% > 地方競馬98.6%**。
  元返し領域の天井は払戻率でなく「本命がどれだけ確実に来るか」で決まり、競艇の1号艇（ほぼ99%が3着以内）が
  競馬の1番人気（3%前後は馬券圏外）より確実だから。**控除率の有利は、届く場所では回収されない。**
- **元返しの外側で最大の比は1.147**（必要1.272）。競艇の crowd_bias 上位1%（1.139）とほぼ同じ。
  「市場は本命を1.15倍ほど過小評価する」は競技をまたいで再現する普遍的な大きさらしい。
- 次: 4月・5月で探索を完成 → **6月以降を確認期間として1回だけ**評価。決着する問いは
  「**複勝・上位5%は98.6%か100%超か**」の1つだけで、±0.7ptに詰めるには約600R＝9〜10か月分（年内で決着）。
  上位5%は1日2.1R。真値が100.7%でも100円で0.7円。**規模の問題は競艇と同じ。**

## 本命6＋穴4＝10点固定・保証つき配分（2026-09-13、`reports/research/mix10.md`、`scripts/run_mix10.py`）

ユーザー指定「10点固定（本命6・穴4）、当たってマイナスになるパターンを無くす、穴の選び方は要相談」。2026年36,924R・確定オッズ。

- **配分**: 保証水準Bに対し 賭け金_i = ceil(B÷オッズ_i÷100)×100、**Σ賭け金 ≤ B** が保証の条件（成立しないレースは見送り）。
  `Σ(1/オッズ) ≤ 1`（＝10点の市場確率合計 ≤ 0.75）が成立条件で、3連単の上位10点なら中央値0.4程度なのでほぼ常に成立する。
  **予算は必ず余る**（保証3,000円に対し実投資2,100〜2,800円）。使い切ったら保証は壊れる。
- 「当たって赤字」は **0.6%** まで落ち、その全件が**公式配当 < 確定オッズ×100**（同着・返還）のレース。
  この不一致は全期間で **613件（1.66%）** あり、既存の全バックテストにも同じだけ効いている。
- **穴の選び方の理屈**: 市場が正しいなら どの10点をどう配分しても期待回収率75%。動かせるのは**選んだ点の比 p/q だけ**。
  穴の賭け金は100円に張り付くので **穴1点の期待払戻 = 75円×比** で配当の大小に依存しない。比は人気薄ほど下がる。
  → 実測も理屈どおり: 人気7〜10 77.0% ＞ 20〜23 76.1% ＞ 30〜33 75.9% ＞ 60〜63 75.0%（前半後半とも同順）。
  13通り試して全部 75.0〜77.6% の範囲。**「穴らしさ」と回収率は正面から対立する**（穴的中時の平均払戻 4,330円 ⇔ 26,219円）。
- **おすすめ（提示済み）**: 本命＝モデル確率上位6、穴＝**人気20〜23**。帯の根拠は `two_modes.md` の人気20〜40、
  その中で最も本命寄りを取る。前半77.1/後半77.5。穴的中は的中の約10%、そのとき平均6,097円（本命的中3,527円）。
- **穴を入れる代金は約3pt**（同じレース・同じ本命6点で7〜10点目だけ差し替え、3通りとも成立したレースに揃えた比較）:
  全レース 78.4→77.2、モデル信頼度上位30% 80.8→77.7、上位10% 84.3→81.1。**自信のあるレースほど代金が高い。**
  最長連敗も 6R → 8R に伸びる。
- **この10点構成は堅いレース向き**: 全レース77.2% / モデル信頼度上位10% 81.1% ⇔ 市場万舟確率上位10%（荒れ）**74.0%**。
  荒れレースでは本命6点が来ず穴4点も薄すぎて当たらない。全レース買うとプラスの日は 0/241日。
- 既存モード（堅い保証つき81.4%、上位厚め83.6%）には勝っていない。すべて確定オッズの数字で、締切前での再測定が要る。

## 本命10点・「当たったら投資の1.5倍以上」の保証（2026-09-13、`reports/research/min15.md`、`scripts/run_min15.py`）

ユーザー指定「本命10本、全体でなく**当たった1レース単位**で払戻 ≥ 投資×1.5。支出はいくら必要か」。2026年36,924R・確定オッズ。

- **成立条件は代数で決まる**: 全点で `賭け金_i×オッズ_i ≥ m×Σ賭け金` ⇔ **Σ(1/オッズ) ≤ 1/m**（丸めを除く）。
  市場確率に直すと `Σq ≤ 1/(m×1.333)`。m=1.0→75%、**m=1.5→50%**、m=2.0→37.5%、m=3.0→25%。
  **配分の制約ではなくレース選択の制約**で、回収率はここからしか動かない。
- **×1.5 に必要な最小支出**: 中央値2,500円・75%点5,200円・90%点13,900円・95%点28,025円（裾が極端に重い）。
  Σ(1/オッズ)が上限ぎりぎりのレースほど丸めを吸収するため支出が跳ね上がる。**支出上限を置いて見送るのが前提。**
- **支出上限×倍率（本命＝モデル確率上位10点）**: ×1.5 は 上限3,000円で成立29%（45R/日・平均支出1,670円・的中34.0%・
  当たって中央+1,790円・回収率75.6%）、5,000円で37%（76.2%）、**1万円で44%（67R/日・2,848円・的中38.1%・+2,280円・77.1%）**、
  2万円で47%（78.9%）。**上限を上げても47%で頭打ち**（代数の壁）。
- **交換条件（上限1万円）**: ×1.0 → ×1.5 で 買えるレース97%→44%、的中51.1%→38.1%、
  **当たったときの利益 中央+520円→+2,280円（4.4倍）**、最長連敗15R→18R、回収率79.0%→77.1%（−1.9pt）。
  **1レースの勝ち分を4倍にする代金は回収率で約2pt。** 前半76.5/後半78.1で再現。
- **×1.5 はモデルが自信を持つレースをほぼ全部落とす**: 信頼度上位10%での成立率は ×1.2で12%、×1.5で0%。
  残るのは本命が割れたレースなので的中率が落ちる。×2.0 は成立16%・的中28.4%・最長連敗30R。
- 本命の選定は モデル上位10点（×1.5で77.1%）> 市場人気上位10点（74.7%）。
- 保証が破れるのは 0.78%（公式配当 < 確定オッズ×100 の同着・返還のみ）。締切前オッズでは本命のオッズが下がるので
  **実運用は ×1.6〜1.7 で組む余裕が要る**（締切前データで要実測）。

## 1日に買うレースの絞り込み（2026-09-13、`reports/research/pick5.md`、`scripts/run_pick5.py`）

土俵は「×1.5・支出上限1万円で保証が成立した16,095R（1日67R）」。そこから1日5本を選ぶ基準を8通り比較。
基準は結果を使わず、モデル確率と確定3連単オッズだけから計算（信頼度／Σq／保証の余裕／1番人気の市場確率／
モデル÷市場／堅さ／モデル期待値／無作為）。

- **回収率は絞り込みでは動かない。** 無作為に1日5本選ぶだけで **71.3〜86.2%**（20回）に散らばり、
  8基準の回収率 72.5〜79.6% は**全部その幅の中**。1日5本＝年1,205Rでは95%区間が±5ptあり、
  **この規模では回収率の優劣を原理的に判定できない**（`stadium_study.md` の場別選択と同じ構図）。
- **確実に動くのは「形」**。C1 モデル信頼度（上位10点の確率合計）で絞ると:
  的中率 38.1%→**46.6%**、最長連敗 18R→**8R**、当たったときの利益 中央+2,280円→**+3,140円**。
  ただし**1レースの支出が 2,848円→4,816円 に増える**（信頼度が高いレースほど Σ(1/オッズ) が 0.667 に近く、
  ×1.5 を成立させるのに大きな賭け金が要る）。回収率が同じなら支出が増えた分だけ損失が増える。
- **おすすめは C1 で1日5本**、理由は回収率ではなく的中率・連敗・1回の勝ち分。**支出上限は別に決める必要がある**
  （C1・上限1万円だと月72.5万円投資・期待損失15万円。上限5,000円にすれば投資は半分以下）。
- 8通り中「前半・後半とも上回った」のは C1 と C4（1番人気の市場確率）だが、帰無でも8通り中2つは通る。
- **2026年の8か月すべてでプラスの月は 0/8。どの基準でも同じ。**

### 本命10点はどう決まっているか（既存の Model 1.0 のまま）
1. **艇別の強さ**: LightGBM の二値モデル3本（1着 / 2着以内 / 3着以内）。特徴量80個（`NUMERIC_FEATURES`）＋
   カテゴリ5個。時間減衰重み 0.5^(Δ年/2)。as-of 原則で対象レースの前日までの情報だけを使う。
2. **120通りの確率**: 位置別割引つき Plackett–Luce（`trifecta.py`）。
   `P(a→b→c) = w_a/Σw × s2_b^λ2/Σ_{j≠a} × s3_c^λ3/Σ_{j≠a,b}`。w=1着確率、s2/s3=2着以内/3着以内確率。
   λ2≈0.7・λ3≈0.6 は検証データの3連単対数尤度を最大化して決める。
3. **校正**: isotonic regression で120通りの確率を「実際に的中したか」に合わせ、レース内で再正規化（`ComboCalibrator`）。
   信頼度は「選んだ点の確率合計」を isotonic で校正した値（`SetCalibrator`）。
4. **本命10点 = 校正後の確率が高い順に10点**。オッズは選定に使わない（配分にだけ使う）。
- 注意: `market_combine.md` の通り、このモデルの1着対数損失は市場（3連単オッズ）に負けている（3.78 vs 3.70）。
  それでも `mix10.md` では本命6点をモデルで選ぶ方が市場人気で選ぶより1〜1.5pt良かった（母集団の違いを揃えた比較でも）。

## 1日5本を「その場で」決める（2026-09-13、`reports/research/online5.md`、`scripts/run_online5.py`）

**訂正: `pick5.md` の「1日5本」はその日の全レースを見てから上位5本を選んでいた＝先読みだった**（ユーザー指摘）。
締切時刻順に1本ずつ判断する形で測り直した。しきい値は探索期間（1〜5月）の分布からだけ作り、確認（6〜8月）に当てる。

| 方式 | 確認期間の回収率 | 的中率 | 最長連敗 | オラクル一致 | 取りこぼし |
|---|---:|---:|---:|---:|---:|
| R0 オラクル（その日の上位5本・先読み・実行不可） | 79.1% | 47.7% | 8R | — | — |
| R1 固定しきい値0.546（本数は変動） | 75.8% | 45.8% | 9R | 72% | — |
| R2 固定しきい値＋5本で打ち切り | 74.0% | 44.9% | 9R | 74% | 1日1.1回 |
| **R3 残り枠に応じた動的しきい値** | **79.3%** | **47.0%** | 9R | **71%** | 1日1.5回 |
| R4 先着5本 | 74.4% | 37.8% | 12R | 9% | 1日50.5回 |

- **答え: 枠を先着で埋めない。残り枠 ÷ この先に残る成立レース数 の分位点をしきい値にする（秘書問題と同じ形）。**
  朝は「今日の上位8%相当」を要求し、終盤で枠が余れば下げる。しきい値表は `online5.md` §5（残り5枠×残り60本→0.541、
  残り1枠×残り10本→0.531、など）。**先読みを一切使わずにオラクル（79.1%）に並ぶ（79.3%）。**
- 「早い者勝ち」だけは避けること。R4 は1日50回取りこぼし、的中率が9pt落ちる。
- R3 の取りこぼしは1日1.5回だが、逃した信頼度の差は中央値0.031で実害はほぼない。
- 本数を固定しない R1 は回収率で差が付かないが、1日最多14本（約7万円）で**投資額が読めない**。資金管理では R3。
- 注意: n=455 で95%区間±8pt。回収率の差は誤差の範囲でもある。確実なのは的中率・連敗・取りこぼしの差。
- しきい値表は3か月ごとに分布を取り直す（自動更新はしない）。×1.5の成立判定にはオッズが要るので、残り本数の
  見積もりは番組表×過去の成立率44%で立てる。締切前オッズでは成立本数が減るのでしきい値は実データで取り直す。

## 5つ目のモード: 3連単（本命10点・1.5倍保証）role=`honmei`（2026-09-13、SW bl-v18、modes_version="modes2"）

`min15.md` / `pick5.md` / `online5.md` の結論をそのまま実装した。

- **選定**: 本線（モデル確率順）の上位10点固定。オッズは選定に使わない。
- **配分**: `min_guarantee_F(odds, 1.5, 10000)` が返す保証払戻 F に対し 賭け金_i = ceil(F÷オッズ_i÷100)×100。
  **成立条件は Σ賭け金 ≤ F**（⇔ Σ(1/オッズ) ≤ 1/1.5 ⇔ 10点の市場確率 ≤ 50%）。
  成立しない／支出が上限1万円を超えるレースは**点数を削らず見送り**（skip_reason='no_guarantee'）。
  **保証を守る限り予算は必ず余る**（保証水準に対し実支出は7割前後）。
- **発火**: 1日5枠。しきい値は `honmei_threshold(残り枠, この先に残る成立見込み本数)` ＝
  `HONMEI_CONF_GRID`（探索期間1〜5月の成立レース9,952本の「本線10点の確率合計」の分位点101個）の
  `1 − 残り枠/残り本数` 分位。**先読みは使わない**。残り枠は `daily.honmei_context` が
  「今日すでに buy した honmei の件数」から、残り本数は「番組表で closed_at がこの先のレース数 × 成立率0.44」から出す。
  枠を使い切ったら skip_reason='slots_full'。
- **しきい値に使う確率は「本線10点の確率合計」**（本体の信頼度＝SetCalibrator とは別物）。
  校正後の120通り確率の合計で、walkforward の ProbStore と live の predict は同じ `combo_cal.transform` を通るので
  スケールは一致する。**モデル版が変わったら分布がずれるのでグリッドを取り直すこと。**
- **直前版 `hon_late`**: 締切2〜4分前のオッズで倍率だけ測り直す（買う/買わないは8分前の判断を引き継ぐ）。
  `lab late-drift` が「8分前→直前の倍率」と「直前に1.5を割った割合」を出す。割るようなら設定の倍率を1.6〜1.7に上げる。
- 実測の目安（`online5.md` R3・確認期間6〜8月・先読みなし）: 回収率79.3%・的中47.0%・最長連敗9R・
  1レース支出4,685円・1日5R → 月の期待損失 約14.5万円。
- 画面: ヘッダは5ボタン（穴狙い／堅い予想／堅い·厚め／**本命10**／複勝·単勝）。390px で収まることを Playwright で確認。
  設定タブに 倍率・支出上限・枠数・点数・成立率の5項目（グリッドは画面には出さず、保存時にそのまま往復する）。
- 出荷前チェック: 71 tests、本番DBコピーで全API 200・NaN無し・設定PUT往復、Playwright 390px で5モード＋4画面
  （横スクロールなし・JSエラーなし）、CLI（modes / late-drift / today-status）を CliRunner で実行。

## 飛躍1: 市場を市場自身で均す（2026-09-13、`reports/research/leap_self.md`、買い方としては不採用・**別の道が見えた**）

ユーザー提案「一回、飛躍した理論で買い目を決めて、そこから引き算で落ち着かせる」。
仮説: **外の情報をゼロにして、市場の内部矛盾だけを使う。** 3連単オッズ q から市場自身の周辺確率
（1着・2着以内・3着以内）を取り出し、本体と同じ位置別割引つき Plackett–Luce に通して内部整合版 q* を作る。
`r = q/q*` が小さい＝市場が自分の構造より安く売っている点。λ2=λ3=0.95（探索期間で KL 最小化）。

- **仮説は明確に否定。しかも向きが逆だった。** 勝った買い目の r は中央値 **1.049**（全買い目 1.008）＝
  市場が構造版より**高く**売っている目ほど実際に来る。対数損失も 市場3.7058 < 自己整合版3.7443、混ぜると単調に悪化。
  割安順に買うと回収率 64〜69%（比0.85〜0.92）、**帰無シミュレーションで 20/20 が実測を上回った**＝でたらめより悪い。
  対照の人気順は 76.6〜77.0%（比1.02）で帰無 0/20。→ **市場の「矛盾」は雑音ではなく知識。この道は閉じた。**
- **代わりに見えた新しい道: 構造の不足は 0.0385 nats。これまでの全努力（0.001 nats）の38倍。**
  市場の周辺確率をそのまま使っても、PL に通すだけで 0.0385 nats 失う。
  **本体 Model 1.0 はまさにこの PL を使っている**（`trifecta.py`）。モデル3.78 vs 市場3.70 の差0.08 のうち
  **約半分は情報ではなく表現力の不足**の可能性が高い（残り半分は周辺確率の精度）。
- **ズレの形は決まり手そのもの**（1着艇×2着艇の平均 r、市場確率で重みづけ）:
  1号艇が逃げたとき 2着は 2号艇1.17・3号艇1.11 が高く、5号艇0.87・6号艇0.82 が安い。
  **4号艇が1着なら 2着 5号艇2.31・6号艇2.11、5号艇が1着なら 6号艇2.25・4号艇1.50、6号艇が1着なら 5号艇1.60。**
  外側が勝つレースでは「引き連れてきた艇」が2着に残る。PL は「2着は個々の強さだけ」と仮定するので原理的に書けない。
- 市場オッズだけから作った粗い6×6補正（探索期間で作成→確認期間に適用、結果は不使用）で
  **0.0385 のうち 0.0102（26%）を回収**。3.7443 → 3.7341。
- **次の一手（提案）: Model 1.3 = `trifecta.py` に 1着艇×2着艇の相互作用を入れる。** 結果データから学習でき、
  場別・コース別に細分もできる。新しいデータは要らない。**手持ちで動かせる最大の一手。**
  ただし `model12_wf.md` の通り対数損失が改善しても回収率は横ばいだった実績があり、0.039 は必要量0.2877 の13%。
  **回収率が動く保証はない。** 買い方としては今回何も採用しない。

## 飛躍2〜4（2026-09-13、`reports/research/leaps2.md`、買い方は不採用・**モデルの形に示唆**）

### 飛躍2: 市場の誤りは「どの3艇か」と「どの順か」のどちらにあるか
3連単は「3艇の組（20通り）」×「並び（6通り）」に分解でき、対数損失も厳密に分解される。
- 市場の情報は **67% が「どの3艇か」、33% が「その並び」**（確認期間）。
- 並びの側にも同じ向きの人気薄バイアス（条件付き確率5%未満で比0.895 → 60%超で1.052）。**誤りは片側に偏っていない。**
- **選び方としては差が出た**: 「最有力の**組**を決め切ってからその中の最有力の並びを買う」が
  「最有力の目をそのまま買う」を上回る。全レース 78.8%（比1.051） vs 76.8%（比1.024）。
  **2つが食い違う11.8%のレースだけだと 83.0% vs 66.2%（+16.8pt、前半+18.7/後半+13.4で両半とも同じ向き）。**
  理屈: 組の確率は並び6通りを合計するのでノイズが均される。組が2番手の目に人気が集まっていたら、その人気は雑音。
- **ただし1点でしか出ない**（上位2組×1並び76.5%、3組76.3%、5組74.5%）。α による滑らかな重みづけでも再現しない
  （`q_set^α × q(並び|組)`、探索で α=1.1 が最良だが k=1 で76.5%）。**買い方としては不採用。**
- 参考: 同じ表で **モデル確率順の上位1点が 81.0%（比1.080、前半81.5/後半80.2）** と市場を4pt上回る。
  対数損失では市場に負けているのに上位端では勝つ、という既知のパターンがここでも出ている。

### 飛躍3: 返還は「情報優位を要しない唯一の補助金」
`期待回収率 = 0.75 + 0.25 × P(返還)` は厳密（中止も取消も払戻100%）。
- 2026年: 中止 541件（1.43%）、一部返還 1,024件（2.71%）。1点あたり返還確率 2.90% → **+0.72pt**。
- **これまでの検証はすべて中止レースを母数から外していた。実運用の回収率は測定値より 0.7pt ほど高いはず。**
- 予測できる: **風8m以上で中止率3.57%（全体の2.5倍）→ +1.35pt**、風8m以上かつ波8cm以上 +1.61pt、
  風10m以上で中止18.9%（n=37）→ +4.73pt。
- **罠を1つ踏んだ**: 気象を「最新の観測」で取ると完走レースは result 相・中止レースは preview 相を見ることになり、
  「強風ほど中止しない」と逆に出た。**必ず phase='preview' に統一すること。**

### 飛躍4: オッズの丸めは帯によって取り分が違う
切り捨て表示で 1〜10倍帯は払戻の0.70%、10〜100倍帯0.15%、100倍以上0.02%を失う。
**低オッズ帯ほど削られる**ので、本命端の優位は見た目より 0.5pt 小さい。

### 総括: 飛躍1と飛躍2が同じ設計を指している
飛躍1=「PLは『誰が1着かで2着の顔ぶれが変わる』を書けず0.0385 nats失う」、
飛躍2=「市場の情報の67%は『どの3艇か』にあり、そこの値付けの方が確か」。
→ **まず『どの3艇が3着以内か』を当て、次に『その並び』を当てる二段構え。これが Model 1.3 の形の第一候補。**
いまの Model 1.0 は1着/2着以内/3着以内の確率から一気に120通りを作る（PL）ので二段構えになっていない。
確実な増分は返還の +0.7〜1.6pt だけだが、**モデルの形を変える理由が2本の独立な測定から出たのは初めて。**

## 飛躍5〜7（2026-09-13、`reports/research/leaps3.md`）— **1-2-3 が最も頑健な単純規則として出た**

### 飛躍5: レースは独立か（同じ日・同じ場の連鎖）— 完全な空振り
「今日ここまで市場の見込みより何本多く1号艇が勝ったか」で5分割しても、実測÷市場は **1.025〜1.042 で真っ平ら**。
勾配ゼロ。全区分1.03前後なのは `crowd_bias.md` の1号艇過小評価そのもの。**セッション内の連鎖は市場が完全に織り込み済み。**

### 飛躍6: 市場がいつも間違える『個人』はいるか — 向きは本物、量は微小
探索（1〜5月）の選手ごとのズレ `(実1着数+20)/(市場見込み+20)` が、**3か月空けた確認（6〜8月）でも単調に効く**:
下位20% 0.955（区間0.920〜0.989）→ 1.035（1.003〜1.068）→ 上位20% 1.031。各区分16,600走。
**ただし同じ物差し（市場オフセット型ロジット）では +0.0005 nats、必要量0.2877の0.18%** で、目的1の5族より小さい。
選手ごとのズレの幅が0.86〜1.15しかなく、logにすると±0.14の微調整にしかならないため。向きは本物だが量が足りない。

### 飛躍7: 買い目の見た目 — **1-2-3 が突出して安い（本プロジェクト最頑健の単純規則）**
オッズ帯で層別しても「連番」が残り、細かく刻んでも残った（同じオッズの他の目との差: 4〜6倍 +0.131、
6〜8倍 +0.078、8〜10倍 +0.124、10〜15倍 +0.118）。**中身を割ると発火の93%が 1-2-3**、除くと77.4%に落ちる。

| 買い方 | レース数 | 的中率 | 回収率 | 比 | 補足 |
|---|---:|---:|---:|---:|---|
| **1-2-3 をオッズ4〜15倍のときだけ買う** | 19,124 | 10.8% | **86.9%** | 1.159 | 前半85.9/後半86.9、帰無 0/20 |
| 1-2-3 を毎レース買う（2026） | 36,251 | 7.4% | 82.2% | 1.096 | |
| **1-2-3 を毎レース買う（2018〜2026・46万R）** | 463,527 | 7.15% | **82.9%** | 1.105 | **9年連続 79〜85%** |
| 市場の1番人気の目を毎レース（2026・対照） | 36,924 | 10.8% | 76.6% | 1.021 | 帰無 6/20 |

- **2018〜2025 の検証は発見に使った2026年のオッズを一切使っていない**（`回収率 = 1-2-3の的中率 × 平均配当 ÷100` で
  結果と公式配当だけから計算できる）。完全に独立なデータで9年連続に再現。
- 他の目は突出しない: 1-3-2 80.7%、1-2-4 73.8%、2-1-3 76.8%、1-4-2 73.5%、1-3-4 74.1%。
- **市場の1番人気（毎レース動く）より、固定の 1-2-3 の方が6pt高い。** 本命過小評価の癖がいちばん濃い場所が 1-2-3。
  `leap_self.md` の「市場は1号艇1着→2号艇2着をPLより1.17倍高く売る」と同じ方向で、**実際はさらに高い**。
- **注意1: それでも86.9% < 100%**（必要な比1.333に対し1.16）。勝てる規則ではない。
- **注意2: 2018年84.7% → 2025年79.1% と緩やかに低下**。市場が直しつつあるのか揺らぎかは9点では決められない。
- オッズ末尾の桁は .0 が1.048で有意に見えるが10桁試して1つなので偶然の範囲。

## 1-2-3 をもう1段絞る（2026-09-13、`reports/research/combo123.md`、`scripts/run_123.py`）

`回収率 = 1-2-3 の的中率 × 平均配当 ÷100` は**結果と公式配当だけで計算でき、オッズが要らない**ので、
46万レース全部を使える。**探索 2018〜2023（32万R）→ 確認 2024〜2026（14万R）で1回だけ評価。**
採用条件は事前に固定（探索で n ≥ 30,000 かつ回収率最大の単独条件1つと2変数の組1つ）。

| 買い方 | 確認（2024〜2026）の回収率 | 95%区間 | 1日あたり | 基準との差 |
|---|---:|---|---:|---:|
| 全レースで 1-2-3 | 80.9% | 78.7〜82.8% | 147.6R | — |
| ＋1号艇と他艇最高の勝率差 0〜1 | **85.4%** | 81.7〜89.5% | 28.5R | +4.5pt |
| ＋1号艇の展示タイム1位 かつ A1級 | **86.0%** | 81.4〜90.5% | 16.2R | +5.1pt |

- **探索→確認で全部が約3pt下がったが、基準も同じだけ下がった**（83.8→80.9）。つまり**条件の上乗せ +5pt はそのまま残った**。
  下がったのは水準で、これは年の違い。年別は 2018〜2023が83〜85%、2024 81.8%、2025 79.1%、2026 82.2%。
- **効く条件の形は「1号艇が強いが圧倒的ではない、静かな水面」**: 勝率差0〜1（探索88.9%）、全国勝率6.5〜7.5（88.6%）、
  展示タイム1位（88.0%）、風1m以下（87.3%）、A1級（87.1%）。**勝率差2以上（独走）は伸びない**＝圧倒的な
  レースでは市場もちゃんと値を付けている。
- 2026年のオッズを足した参考値（post-hoc・2026のみ）: 展示1位×A1 で 91.8%（比1.224、n=3,812）、
  勝率差0〜1 × 4〜15倍 で 89.8%（n=5,185）。**帯は条件の上に大きくは足さない**（条件と重なっている）。
- **それでも 86%。必要な比1.333に対し1.147。** 1日16R×100円＝1,600円、月の期待損失 約6,700円。
  **「いちばん負けにくい3連単の買い方」であって勝てる買い方ではない。**
- **訂正**: `leaps3.md` で「2018→2025 で緩やかに低下」と書いたが、**2026年は82.2%に戻っている**。
  2024〜2025が低く2026が戻った形で、低下傾向と言い切れる形ではない。9点では判定できない。

## 飛躍8〜10（2026-09-13、`reports/research/leaps4.md`）

### 飛躍8: 1-2-3 は特別か —— **特別ではない。軸は「出やすさ」**
120通り全部の「毎レース買う回収率」を46万レースで出せる（結果と公式配当だけ）。
- 転倒数（艇番順からの崩れ）では勾配なし: 転倒0 65.4% / 転倒1 **66.6%** / 転倒2 57.7% / 転倒3 53.4%。
  1号艇1着に限ると 転倒0 71.8% vs 転倒1 72.5% でほぼ同じ。**整列そのものは効いていない。**
- **本当の軸は出現率**。出現率で5分割すると 46.8% → 57.4% → 63.3% → 66.2% → **72.5%** と単調、
  出現率(log)との相関 **+0.875**。**人気薄バイアスが「目」の単位で現れている。**
- 1-2-3 が最強なのは46万レースで最も出やすい目（7.15%）だから。2位は 1-3-2（5.24%・80.7%）。
- 最悪は 6号艇1着の目（39〜43%）。

### 飛躍9: 決まり手 —— **上限は巨大、市場を超える分はゼロ**
- 決まり手を知ると120通りの不確かさが **0.7168 nats** 減る＝**市場が3連単に持つ情報の66%**。
- レース前の情報で決まり手を当てると出現率基準より **+0.0619 nats**（必要量の21.5%）＝単体では過去最大。
- **だが市場の値段に乗せると +0.0003 nats（0.10%）。** γ=0.15、確認3.6887 vs 市場3.6890。
  **市場は我々が予測できる範囲の決まり手をすでに全部知っている。**
- **分けて理解する**: 0.0385 nats（`leap_self.md`）＝モデルの*構造*が捨てている分＝**市場に近づくため取り戻せる**。
  0.0003 nats＝決まり手予測が*市場を超える*分＝**取り戻せない。壁はここ。**

### 飛躍10: 市場の厚さ —— **空振り。代理変数が間違っていた**
Σ(1/オッズ) を薄さの代理にしたが、**本命の市場確率との相関 +0.803**（本命オッズと −0.689）で
実体は「本命がどれだけ堅いか」。丸め損は低オッズの目で大きく Σ は 1/オッズ 重みなので当然だった。
Σ上位20%で1-2-3が90.9%になるのは堅いレースの話の言い換え。**市場の厚さは売上高が要るが公式は非公表＝測りようがない。**

## 飛躍11〜13（2026-09-13、`reports/research/leaps5.md`）— 3本とも否定、うち1本は既存設計の裏づけ

### 飛躍11: 複勝は1レース1点、という前提は正しいか —— **正しかった。1点が最適**
市場の3着以内確率（3連単プールから周辺化）の順に買うと **1位 93.6% / 2位 88.5% / 3位 87.9% / 4位 85.2%** と単調に低下。
まとめ買いは薄めるだけ（上位2艇 91.0%、上位3艇 90.0%）。**点数を増やすと必ず下がる。**
理由は元返しの分布で、**的中時に100円で返る割合が 1位 50.1% → 2位 15.5% → 3位 4.4%**。
`sweet_spot.md` の「元返しは胴元からの補助金」がそのまま効き、**補助金は最有力の1艇にだけ集中している**。
しきい値つきでは 3着以内確率0.95以上の1位艇が **98.0%**（n=1,824）。**本番の複勝モード（1点・0.90以上）の設計は正しい。**

### 飛躍12（番組の種類）・飛躍13（曜日・時間帯）—— **どちらも揺らぎの範囲**
準優勝戦 1.154、一般戦の1-2-3 1.304、土曜 1.082、14〜15時締切 1.097、ナイター 1.057 が目を引くが、
**同じ大きさの群を無作為に200回作ると同じ幅が出る**（準優勝戦の観測1.154 に対し無作為の範囲 0.782〜1.254 など）。
前半・後半で符号が揃わないセルも多い。**番組や曜日で群衆の賢さが変わる証拠は無い。**
教訓: 1レース1点・n=5,000 程度では比の95%区間が ±0.05〜0.10 になる。**この粒度で区分を探すと必ず何かが有意に見える。**
`stadium_study.md` で踏んだ罠と同型。今回は事前に無作為対照を置いたので引っかからずに済んだ。

## 飛躍14〜16（2026-09-13、`reports/research/leaps6.md`）— 飛躍シリーズ完結

### 飛躍14: 番組が人工的に堅くしたレース（企画レース）—— **否定。堅さは値段に入っている**
企画レース12,724本（モーニング・ツッキー・サンライズ・進入固定・ピンクル・ガチ勝ち8）。
**1-2-3 の出現率は ×1.35（7.08→9.53%）、配当は ×0.76（1,169→893円）、積は 1.028。**
探索(2018〜23) 88.6% → 確認(2024〜26) 78.5%。それ以外は 83.6→81.0%。
**帰無対照（それ以外から同サイズを2,000回抽出）**: 探索88.6%以上が出る確率 **0.135**、
10.1ptの落差が出る確率 **0.154**。**どちらも揺らぎの範囲。** 1-2-3 が来るのは1,213本だけなので
回収率の区間が±10pt級になり、この粒度では判定不能。
**進入固定戦隊 全期間104.0%（n=1,154、区間87〜122%、探索118.1→確認82.8）は `stadium_study.md` の
びわこ102%と同型の罠。** 系統別の探索→確認は符号すら揃わない（ツッキー89.6→56.8、ガチ勝62.7→77.2）。

### 飛躍15: 位置別の値付け —— **シリーズ最後の陽性。人気薄バイアスは後ろの位置ほど急**
3連単プールから「ちょうど n 着になる確率」を周辺化（2026年36,924R）。全体は定義上1.0000なので
**市場確率の帯を揃えて位置を比べる**（揃えないと「3着は低確率セルが多い」だけの話になる）。

| 帯 | 1着 | 2着 | 3着 |
|---|---:|---:|---:|
| 3〜5% | 0.894 | 0.787 | **0.686** |
| 5〜8% | 0.940 | 0.922 | **0.806** |
| 25〜35% | 0.999 | 1.021 | **1.059** |
| 35%〜 | 1.034 | **1.089** | — |

**傾き（log比 vs log q）: 1着 +0.102 / 2着 +0.182 / 3着 +0.192。帰無（y~Multinomial(q)）20回は
いずれも ±0.05 以内で 20/20 が外。** → **市場は「誰が勝つか」はよく値付けし、「誰が2着・3着に
残るか」で人気薄バイアスが約2倍に開く。**
- **ただし買い方には使えない**: 位置別の校正表（2026前半）で後半の120通りの実測比を予測すると
  相関 **0.473** 止まりで、**1-2-4 を予測1.082 で 1-2-3 の1.076 より上に置いてしまう**（実測は1.021 vs 1.132）。
  1-2-3 の超過分の約6割しか説明できない。
- **残りは `leap_self.md` の「1着が誰かで2着の顔ぶれが変わる」相互作用。Model 1.3 の2本目の独立な裏づけ。**

### 飛躍16: 癖の賞味期限 —— **薄れていない**
年ごとの「出現率(log)と120通り回収率の相関」は 2018〜2026 で **+0.703〜+0.829**、
最小二乗の傾きは **年あたり +0.0055（むしろ上向き）**。回収率の散らばりも −0.0002/年で横ばい。
最頻10目の平均だけ −0.21pt/年（75.1→72.1%）だが 1-2-3 自体は 84.7→79.1→82.2% と上下しており
9点では傾向と言えない（`combo123.md` の訂正と同じ）。
**「出やすい目ほど安い」に賞味期限は無い。ただし82%が82%であり続ける意味であって、100%に近づく意味ではない。**

### 飛躍シリーズ16本の総括
**16本試して、市場を超える情報は1つも見つからなかった**（決まり手 +0.0003 / 選手ごとのズレ +0.0005 /
目的1の5族 ≈0.001 / 万舟予測 +0.0023、必要量0.2877に対し最良0.8%）。見つかったのは3種類だけ:

| 種類 | 中身 | 大きさ | 使えるか |
|---|---|---|---|
| ①情報の要らない補助金 | 返還、元返し | +0.7〜1.6pt / 複勝1点に集中 | **使える。実装済み** |
| ②市場の値付けの癖 | 出やすい目ほど安い、後ろの位置ほど急 | 比1.16が上限 | 1.333に届かず**負けが小さくなるだけ** |
| ③こちらの表現力不足 | PL が書けない1着×2着の相互作用 | **0.0385 nats** | **取り戻せる（市場に近づく分だけ）** |

**③だけが未着手で、全努力（0.001 nats）の38倍。** 飛躍1・飛躍2・飛躍15 の3本が独立に
**「まず3艇の組、次にその並び」の二段構え（Model 1.3）** を指した。ただし 0.0385 は必要量の13%で、
`model12_wf.md` の通り対数損失の改善が回収率に出なかった実績もある。**市場に追いつく話であって超える話ではない。**

## 更新スクリプトの事故と対策（2026-09-14、`deploy/update.sh`）

7:37 に `sudo bash deploy/update.sh` が途中で止まり、その日の予想が一部欠けた。原因は3つ重なっていた。

- **罠1: SSH が切れるとビルドが死ぬ。** `update.sh` はフォアグラウンド実行なので SIGHUP で落ちる。
  順序が `git pull` → `build` → `up -d` なので、**pull だけ成功してイメージは古いまま、コンテナは上がらない**
  という中途半端な状態になる。→ **必ず `setsid nohup` で親から切り離して実行する。**
- **罠2: 2GB VPS では scheduler(mem_limit 1400m) を止めないと build が OOM する。** sshd ごと詰まるので
  罠1と同時に起きる（SSHクライアントが落ちたのは結果であって原因ではない）。
  → build 前に `docker compose stop scheduler`。**失敗しても ERR トラップで上げ直す**（止まったままが最悪）。
- **罠3: `git pull` が実行中の `update.sh` 自身を書き換える。** bash は実行中のファイルを逐次読むので、
  行数が変わると次の read がズレた位置に落ちる。→ **pull 直後に `exec bash "$0"` で自分を読み直す**
  （`BOATLAB_UPDATE_STAGE` で1回だけ）。

**新しい実行手順**:
```
cd /opt/boatlab
sudo setsid nohup bash deploy/update.sh > /tmp/update.log 2>&1 < /dev/null &
sleep 3; tail -f /tmp/update.log        # Ctrl-C で抜けてもビルドは続く
```

**欠けた予想は埋めない。** `predict_pending` は締切4分前を切ったレースを対象外にするので、
停止中に締切を通過したレースは恒久的に未予想のまま残る（追記専用・結果より前にしか保存しない原則どおり）。

**教訓**: 出荷前チェックリストは「アプリのコード」に向いていて、**運用スクリプト自体を検証対象にしていなかった**。
今回は stub した git/docker で「正常系・build失敗系・自己書き換え系」の3経路を手元で実行してから出した。

## 保証の検算コマンド（2026-09-14、`lab guarantee-check`）

honmei の初日（9/14）に「当たったのに投資を下回った」が1件出た（三国8R、投資1,600円→払戻1,480円＝×0.93）。
`late-drift` では 8分前→直前の倍率が 1.500→1.515 で**下落は 0.0%** だったため、オッズの動きでは説明できない。
原因を推測せず切り分けるために `lab guarantee-check --days N --role honmei` を追加。

保証は組み立て時に全点で `賭け金_i × オッズ_i ≥ 倍率 × Σ賭け金` を満たすことで成り立つので、破れる経路は3つしかない。

- **A 組み立て不良** … 保存オッズ（`prediction_selections.odds_at_pred`）で再計算しても倍率に届かない＝**実装の不具合**
- **B 配当の下振れ** … 公式配当 < 予想オッズ×100。締切までの下落、または同着・返還（`mix10.md`: 全期間1.66%）
- **C 返還** … 返還点が出ると投資が減るので倍率は**上がる**側。破れる原因にはならない

出力は1レースずつ「**日付**・場・R・時刻」「投資 → 払戻・実倍率」「的中目の賭け金・予想オッズ・公式配当と乖離率」
「組み立て時の最小倍率（再計算）」「返還点数」「原因」。最後に的中数と保証割れの内訳。

**`--days N` は「今日からN日前<ins>以降</ins>」なので N=1 でも2日分入る。** 最初の版は日付を出して
いなかったため、`lab modes`（今日1日）の発火5Rと突き合わせて「推奨に入っていないレースがある」と
混乱させた（ユーザー指摘）。場とRだけでは毎日ある同名レースを区別できない。
**日別の集計と対象期間を先頭に出し、各行にも日付を付けた。**

出荷前チェック: 一時DBに OK／A／B／外れ／オッズ未保存(None)／返還あり の6ケースを合成して全経路を確認。
**教訓**: 環境変数名を `DATABASE_URL` と取り違えて手元の実 lab.db に向けかけた（`BOATLAB_DATABASE_URL` が正しい）。
`with eng.begin()` のロールバックに救われたが、テストDBに向いていることを最初に assert で確かめること。

## 保証が破れる本当の理由: 当たった目だけが深く下がる（2026-09-14、`lab winner-drift`）

`guarantee-check` の結果、honmei 初日の保証割れ2件は**どちらも B（配当の下振れ）で、A（実装の不具合）ではなかった**。
組み立て時の最小倍率は再計算で ×1.53 / ×1.50 で、保証は正しく成立していた。

| レース | 予想時オッズ | 公式配当 | 乖離 |
|---|---:|---:|---:|
| 三国 8R | 12.0倍 | 7.4倍 | **−38.3%** |
| 下関 10R | 6.2倍 | 5.4倍 | **−12.9%** |

**`late-drift` の「1.5割れ 0.0%」と矛盾しない。** あれは10点の**最小倍率**（保証が今も組めるか）を見ており、
払戻を決めるのは**実際に当たった1点**。別の量だった。

**仮説: 当たる目には締切直前の資金が集まるので、当たった目のオッズは平均より深く下がる。**
そうならば ×1.5 は揺らぎではなく**偏り**として不足する（構造的に楽観）。
`odds-drift` の帯別下落は 5〜10倍 −8.1% / 10〜20倍 −8.7%（全買い目の平均）で、今日の2件はそれより深い。

`lab winner-drift --days N --mult 1.5` で直接測る。予想に保存した `odds_used` と翌朝の確定オッズ
（`turnmark_final`）を突き合わせ、**同じオッズ帯の中で**当たった目とそれ以外の下落率を比べ、
「確定時点で ×mult を守るには予想時に ×いくつで組む必要があるか」まで出す（中央値版と25%点版）。
9/11 から実オッズが貯まっているので**待たずに答えが出る**。

出荷前チェック: 一時DBに「当たった目 −20%・それ以外 −5%」を仕込み、
出力が −20.0% / −5.0% / 必要倍率 ×1.88（＝1.5÷0.8）を正しく復元することを確認。
推定オッズのレース・確定オッズ未取込のレース・該当ゼロの3経路も確認。

## honmei の保証倍率を 1.5 → 1.62 に変更（2026-09-15、SW bl-v19、modes_version="modes3"）

### 測定: 当たった目は他より深く下がる（`lab winner-drift`、9/11以降の実オッズ）
**当たった目の下落は中央値 −7.4%。** → 確定時点で ×1.50 を守るには予想時に **×1.62**、
4回に3回守るなら **×1.76** で組む必要がある。`late-drift` が「1.5割れ 0.0%」だったのと矛盾しない
（あれは10点の**最小**倍率＝保証が組めるか、こちらは**実際に当たった1点**）。

### 倍率ごとの交換条件（2026年36,924R・確定オッズ・上限1万円）

| 倍率 | 成立率 | 本/日 | 的中率 | 回収率 | 探索／確認 |
|---|---:|---:|---:|---:|---:|
| ×1.50 | 43.6% | 66.8 | 38.1% | 77.1% | 76.5／78.1 |
| **×1.62** | 34.0% | 52.1 | 35.2% | 76.2% | 74.2／79.3 |
| ×1.76 | 25.8% | 39.5 | 32.7% | 76.8% | 75.6／78.8 |
| ×2.00 | 16.3% | 25.0 | 28.4% | 75.9% | 74.9／77.6 |

- **訂正: 「倍率を上げると買えるレースが減る」は honmei では実質的な制約にならない。** 枠は5本しか使わず、
  ×1.76 でも39.5本/日ある。**実際に失うのは的中率。** 回収率はどの倍率でも 75.9〜77.1% で動かない。
- **R3（動的しきい値・5枠・確認期間455R）で再測定**: ×1.50 は記録値（79.3%・47.0%・4,685円）を完全再現。
  ×1.62 は **回収率82.4%・的中44.4%・支出4,590円・最長連敗10R**。回収率の +3.1pt は n=455（±8pt）で
  **改善とは言えない**。確かなのは 的中 47.0→44.4%、連敗 8→10R の交換条件の方。`MEASURED["honmei"]` を更新。
- 「当たって元本割れ」（1.0倍未満）は別の話で −33%以上の下落が要る（9/14 三国8R の −38.3% がそれ）。
  **いま直したのは「1.5倍」の方であって「元本割れしない」の方ではない。**

### 実装: 倍率としきい値表は必ずセットで動く（静かに壊れる罠）
**倍率を上げると `Σ(1/オッズ) ≤ 1/倍率` を満たすレースだけが残り、その母集団の確率合計は下にずれる**
（中央値で ×1.62 は −0.024、×1.76 は −0.046）。**古い表のままだとしきい値が高すぎて枠が埋まらない。**
- `HONMEI_GRIDS = {1.50, 1.62, 1.76: (分位表101点, 成立率)}` を用意し、`honmei_grid_for(mult)` が
  対応する組を返す（無い倍率は最も近いものにフォールバック）。
- `modes_from_settings` が、設定で倍率だけ変えられた場合に表と成立率を付け替える。
  表を明示保存した設定はそのまま尊重する。`tests/test_honmei.py` に固定（12 tests）。
- 画面・CLI の「1.5倍保証」は直書きをやめ、**設定値から出す**（`HMULT` / `hm()` / 設定画面は `mp` から直接）。
  倍率を変えると表示も追随することを Playwright 390px で確認（1.62 と 1.76 の両方）。

### 出荷前チェック
18 tests、本番DBコピーで `/api/{modes,today,stats,settings}` 200・NaN無し・設定PUT往復で表が付け替わること、
Playwright 390px で5画面（横スクロールなし・JSエラーなし・古いラベル無し）、CLI を CliRunner で実行。
**罠**: SQLite の `-wal` を消さずに DB をコピーすると前の書き込みが復活する（1.62 のはずが 1.76 に見えた）。
テストDBを作り直すときは `db` `db-wal` `db-shm` の3つを消す。

## honmei 2日目（2026-09-15）: 倍率1.62は効いたが、枠が朝で尽きる（`lab honmei-pace`）

**倍率の変更は反映された**（設定未保存だったのでコードの既定値が効いた）。発火5本ちょうど、
1レース支出 **4,540円**＝再測定の予測値 4,590円とほぼ一致。しきい値表の付け替えも動いている。

**保証割れ 3/5（60%）は想定どおりで不具合ではない。** ×1.62 は「当たった目の下落の**中央値**」を
埋める設定なので、**設計上およそ半分は届かない**。4回に3回にしたければ ×1.76。
（9/15 徳山2R: 予想7.6倍 → 公式6.2倍 −18.4%、保証×1.63 に対し実×1.39。原因は B のみ、A は0件。）

### 枠が朝で尽きる（9/14 は13:34、9/15 は **11:31**）
バックテストでは枠を使い切るのは中央値 17:55（設定0.34）〜17:00（真の残り本数）なので、**実運用が異常に早い**。

**成立率の見積もりが実際とずれている**（`honmei-pace` の日別行で確認できる）:

| 日 | 倍率 | 実オッズ | 成立 | 実際の成立率 | 設定値 |
|---|---:|---:|---:|---:|---:|
| 9/14 | ×1.50 | 166R | 105R | **63.3%** | 44% |
| 9/15 | ×1.62 | 151R | 89R | **58.9%** | 34% |

理由は明快で、**表は確定オッズで作り、実運用は締切前オッズ**。締切前は本命のオッズが高めなので
`Σ(1/オッズ) ≤ 1/倍率` を満たしやすい。`races_left` を小さく見積もる → しきい値が下がる → 早く埋まる。

**ただし成立率だけでは説明が付かない**（バックテストで0.34にしても17:55）。同じ「確定 vs 締切前」の
ずれは**成立レースの顔ぶれ**も変える（本命が堅い＝確率合計が高いレースが締切前だと成立しやすい）ので、
**しきい値表そのものが実運用とずれている可能性**がある。

`lab honmei-pace --days N` で A（成立率）・B（表のずれ）・C（その日の並び）を切り分ける。
記録済みの `flags.p10 / threshold / slots_left / races_left` を使うので**追加のデータ取得は要らない**。
出力は日別の発火時刻・確率合計・しきい値・実際の成立率、および
**実運用の確率合計の分位 vs 表の分位**（中央値が表の何%分位に相当するか）。
出荷前チェック: 合成データで B（表より高い）・一致（A/C）・該当なしの3経路を確認。

## 確定予想を締切に近づけるか（2026-09-15、判断は測定待ち）

ユーザー提案「いまは実際に賭けていないのだから、確定予想を締切に近づけては」。

**技術的には簡単**（`scheduler` の2箇所）:
- `predict_pending(..., min_minutes_before_close=4, max_minutes_before_close=10)` → 2〜4 等
- 3連単オッズの取得窓 `6 <= mins <= 12`（予想が使うのはこの窓のオッズ）
直前2〜4分のオッズ取得と記録（`record_late_modes` → `hon_late` 等）は**既に動いているので配管は要らない**。
公式サイトのレート制限（3秒間隔・1日1,000回）も、既に1レース最大4回取っていて余裕がある。

**効く見込み（未確定）**
1. **枠が朝で尽きる問題には効く可能性が高い。** しきい値表と成立率は確定オッズで作ってあり、
   実運用が締切前オッズなのがズレの原因（成立率 34% 設定 vs 実測 59%）。
   **予想を締切に寄せるほど実運用のオッズが確定に近づき、表の前提に合う。**
2. **保証割れに効くかは不明。** `late-drift` の「8分前→直前で1.5割れ 0.0%」は10点の**最小**倍率の話で、
   払戻を決める**当たった1点**の話ではない。当たった目の下落 −7.4% が、8分前→直前と直前→確定の
   どちらで起きているかは未測定。

**測る手段を用意した**（`lab winner-drift` に追加）。`role='honmei'`（8分前）と `role='hon_late'`（直前）は
同じレースの `odds_used` を別時刻で持っているので、**当たった目について 早→確定 と 直前→確定 を
同じレース集合で比較**し、それぞれ「×1.5 を守るのに必要な組み立て倍率」まで出す。
追加のデータ取得は不要（9/13 から貯まっている）。

**判断の順序**: 先に測る → 縮むなら時刻を動かす（倍率を上げずに済む）／縮まないなら時刻は据え置き、
倍率で対応する。**時刻を動かすと過去の記録と条件が変わる**ので、動かすなら `settings_versions` で
区切りを付け、前後を混ぜて集計しないこと。

**据え置く理由になりうる点**: 実運用で買うなら発注時間が要る。3連単21点（穴）を2分で入れられるかは
別の検証が要る。**「賭けない前提で最適化した数字」は、賭けるときに再現しない**ので、
時刻を動かすなら「実際に発注できる時刻」を上限にする。

出荷前チェック: 合成データで「縮む（−20%→−7.7%）」「縮まない（−20%→−20%）」「直前版がまだ無い」
の3経路を確認。18 tests。

## 訂正: 必要倍率は「全帯の中央値」で決めてはいけない（2026-09-16）

9/16 の `winner-drift --days 7`（当たった目174件）で、9/15 の判断の土台が間違っていたことが分かった。

| 予想時の帯 | 当たった目 中央値 | 件数 | それ以外 中央値 |
|---|---:|---:|---:|
| 4〜7倍 | **−14.9%** | 12 | −7.2% |
| 7〜12倍 | **−15.1%** | 19 | −8.3% |
| 12〜20倍 | **−15.3%** | 32 | −6.0% |
| 20〜50倍 | −16.7% | 55 | +1.9% |
| 50倍〜 | **−7.8%** | 56 | +52.4% |
| 全体 | −13.8% | 174 | +41.8% |

- **「それ以外 +41.8%」は人気薄のオッズが締切に向かって上がるため**（`odds-drift`: 200倍〜 +71.1%）。
  全体の「差 −55.6pt」は帯の構成比を見ているだけで意味が無い。**比較は帯ごとにする。**
- **保証を壊すのは本線10点の中の当たり目で、その予想時オッズは概ね 2〜30倍。**
  その帯では下落が **−14.9 / −15.1 / −15.3%** と揃っており、必要倍率は **×1.76**。
- **全帯を混ぜた中央値 −13.8% は、50倍〜（下落が浅く件数が多い）に引っ張られて甘く出る。**
  9/15 に ×1.62 を採用した根拠はこの混ぜた中央値だった＝**選定が甘すぎた。**
- 推定は1日で ×1.62 → ×1.74 と動いた。**毎日追いかけない。** 帯を絞った値の方が安定している
  （3帯が ±0.2pt に揃う）。

`lab winner-drift` に「本命10点の当たり目が乗る帯（2〜30倍）だけで見る」節を追加。
10%/25%/50%/75% 点それぞれの必要倍率と、**倍率ごとに「当たって元本割れ」になる割合**を出す。
`aw`（帯順）と `aq`（当たった目の予想時オッズ）の並びを揃えるのを一度間違えたので、帯ごとに集めてから
同じ順で連結している。出荷前チェック: 合成データ（2〜30倍は −15%、50倍〜は −5%）で
帯の値 −15.0%・必要倍率 ×1.76 を復元し、混ぜた中央値が −10.0% と甘く出ることも確認。

**倍率はまだ変えない。** 先に「予想を締切に近づけたら縮むか」（同 `winner-drift` の末尾）を見る。
縮むなら時刻を動かす方が安い（的中率を失わない）。縮まないなら ×1.76 にする。

## 確定予想を締切 2〜4分前 に移した（2026-09-16、`scheduler.FINAL_MIN/FINAL_MAX`）

### 決め手: 下落の7割は「8分前→直前」で起きていた
`lab winner-drift --days 7`（当たった目176件、同じレースで比較）:

| 予想の時刻 | 当たった目の下落 中央値 | ×1.5 に必要な組み立て倍率 |
|---|---:|---:|
| 8分前 | −13.8% | ×1.74 |
| **直前（2〜4分前）** | **−4.2%** | **×1.57** |

**時刻を寄せるだけで下落の約7割が消え、的中率は1ptも払わない。** しかも必要倍率が ×1.57 まで下がるので、
**いまの ×1.62 のままで中央値を守れる**（倍率を上げる必要がない）。

### 倍率で対応する道が割に合わないことも確定した
本命10点の当たり目が乗る帯（2〜30倍・89件）の分位と、倍率ごとの「当たって元本割れ」割合:

| 倍率 | 当たって元本割れ | 的中率の代償 |
|---|---:|---|
| ×1.50 | 22% | — |
| ×1.62 | 18% | −2.9pt |
| ×1.76 | 13% | −5.4pt |
| ×2.00 | **7%** | −9.7pt |

**×2.00 まで上げても7%残る。** 締切前オッズで組む以上「当たってマイナスにならない」は
**原理的に保証できない**（保証できるのは確定オッズに対してだけ）。倍率はそれを買い取れない。
分位も 10%点 −45.9%／25%点 −30.4%／中央 −15.5%／75%点 +3.9% と裾が重く、倍率で覆うのは非効率。

### 実装
- `FINAL_MIN, FINAL_MAX = 2, 4`（旧 4, 10）。スケジューラは **30秒ごと**に回るので2分幅でも4回試行できる。
  この窓の直前に同じループで3連単オッズを取り直しているので、**予想はその直前オッズを使う**。
- **取りこぼし救済**: `FINAL_RESCUE_MIN = 0.7`。窓が狭いぶん混雑で取り逃がす恐れがあるので、
  締切0.7〜2分前に未作成のレースを拾う。「どのレースにも確定予想がある」を壊さないため。
  何分前に作られたかは `post_time_at_pred − created_at` で分かるので、後から通常分と区別できる。
- `record_late_modes` の呼び出しを外した（確定予想がこの窓に来たので二重になる）。関数とテストは残す。
- `predict_pending` の分数引数を `int` → `float`（0.7 を渡すため）。
- `winner-drift` の「8分前 vs 直前」節は `hon_late` 依存をやめ、**オッズのスナップショット同士**で比べる形に
  変更（6〜12分前と2〜4分前の2回は今後も取り続ける）。**時刻変更をまたいでも同じ物差しで見られる。**

### 注意
- **9/16 を境に条件が変わる。** 前後を混ぜて集計しない。境目は `post_time_at_pred − created_at` で判別できる。
- **実運用で買うなら発注時間が2〜4分しかない。** 本命10点はフォーメーションで入るが、
  穴の21点は急ぐ。「賭けない前提で最適化した数字」にしないため、発注できない時刻までは寄せていない。
- 倍率は **×1.62 のまま据え置き**（時刻変更後に `winner-drift` で再確認してから判断する）。

## 場ごとの「1号艇の次」の艇と複勝100円（2026-09-18、`reports/research/lane2_place.md`）

ユーザー依頼「各場で2連対率が1号艇の次に高い艇を分析し、その艇に複勝100円を買い続けたら」。
2018〜2026・485,064レース、`result_entries` の着順と `results.payouts.place` の実払戻だけで計算。

- **訂正: 競艇の複勝は2着以内**（払戻は2艇。3艇は同着のみ。place の艇が実際の1〜2着と99.9%一致）。
  `leaps5.md` で「3着以内」と書き市場の3着以内確率で並べていたのは誤りで、同ファイルに訂正を入れた。
  回収率の数値は実払戻から出しているので値自体は正しい。本番の複勝モードは正しく2着以内確率を使っている。
- **24場すべてで 1位＝1号艇・2位＝2号艇。例外なし。** 「場によって2番手が違う」は否定。
  2位と3位の差が薄いのは芦屋(+2.3pt)・蒲郡(+2.9)・常滑(+4.1)だが順位は入れ替わらない。
- **2号艇に複勝100円: 全場 86.2%**（475,269R、的中39.2%、的中時平均220円、元返し14.3%、年別85.0〜87.5%）。
  **100%超えの場はゼロ**（最高 福岡93.0%・最低 芦屋80.4%）。
- **艇番別は単調**: 1号艇94.5% > 2号艇86.2% > 3号艇81.8% > 4号艇79.3% > 5号艇72.3% > 6号艇55.3%。
  **「1号艇の次を狙う」は1号艇を買うより8.3pt悪い。** `longshot.md` の人気薄バイアスが艇番の単位で出ている。
- **場の差は本物だった**（探索2018〜23→確認2024〜26 の順位相関 **ρ=+0.569**、帰無200回で実測以上は **1/200**）。
  **`stadium_study.md` の「場の差は無い」は2026年のみ・1場257Rでの結論で、47.5万レースなら検出できる。**
  正体は **2号艇の2連対率と回収率の相関 +0.725**（1号艇の2連対率とは −0.412）＝
  **2号艇が強い場ほど、その強さがオッズに織り込まれていない。**
- 前向きにも使える: 2連対率で選んだ上位3場（福岡・徳山・津）は確認期間 **90.3%**、
  無作為3場200回の 82.3〜89.6% の外。ただし **90.3% で100%には届かない**。
- **結論**: 艇番を固定する買い方は、レースごとに最有力艇を選ぶ買い方（`sweet_spot.md` の複勝上位10% 99.1%）に
  勝てない。場の差という新しい事実は得られたが、大きさが足りない。

## 1号艇の複勝を場別に（2026-09-18、`reports/research/lane1_place.md`、`scripts/run_lane_place.py <艇番>`）

- **全場 94.5%**（473,775R、的中72.5%、的中時平均130円、元返し46.3%）。
  **最高 江戸川 100.03% ／ 最低 戸田 89.9%。** 2号艇（86.2%）より8.3pt良い。
- **江戸川の100.03%は「100%超え」とは言えない**: 95%区間 98.0〜102.0%、100%以上の年は5/9、
  探索(2018〜23) 100.6% → 確認(2024〜26) 98.8%。**9年17,928レースかけて「ほぼトントン」が確認できた**という結果。
  `sweet_spot.md` の複勝上位10% 99.1% に次ぐ水準で、**固定ルールとしては最も100%に近い**。
- **場の差は本物**（ρ=+0.645、帰無200回で 0/200）。**ただし1号艇では強さで説明できない**
  （2連対率との相関 +0.309、元返しとは −0.021）。2号艇では +0.697 で「強い場ほど織り込まれていない」と
  説明できたが、1号艇の場の差の中身は未解明。
- 江戸川は1日約7.3レース。100円で期待利益ほぼゼロ、増額すれば自分でオッズを潰す（規模の壁は同じ）。

### 集計の罠（この分析で踏んで直した）
- **2連対率の分母に中止・不成立レースを入れてはいけない。** `result_entries` には成立しなかった
  レースの行も残る。不成立率は **江戸川 7.64% ⇔ 多摩川 0.22%** と場ごとに大きく違うので、
  分母に残すと**荒れる場の2連対率だけが不当に低く出る**（江戸川の1号艇で −5.1pt）。
  `results.payouts.place` が存在するレース（＝成立した）に母集団を揃える。順位（1位1号艇・2位2号艇）は不変。
- レポート生成を艇番でパラメータ化したとき、§5の相関文だけ2号艇の数字が残った。
  **置換は必ず assert を付けて失敗に気づけるようにする。**

## 本番7日分のCSVレビュー（2026-09-18、9/12〜9/18・4モード）

**時刻変更（9/16）は効いたが、予測ほどではない。**

| | 前(〜9/15) | 後(9/16〜) | 予測 |
|---|---:|---:|---:|
| 当たった目の 予想時÷確定 | 0.860（−14.0%） | **0.906（−9.4%）** | −4.2% |

`winner-drift` の予測 −4.2% に対し実測 −9.4%。**改善は約3分の1しか出ていない。**
原因候補（未検証）: 予想が窓の上端（4分前）に寄っている／直前オッズの取得に失敗して8分前の
オッズで作られたレースが混ざっている／n=86 の揺らぎ。**本番で `lab winner-drift --days 3` を
（9/16以降だけで）回せば切り分けられる。**

| モード | 前 発火/回収率 | 後 発火/回収率 | 目安 |
|---|---|---|---:|
| 穴 | 91本 / 79.6% | **18本 / 17.5%** | 83.3% |
| 本命10 | 14本 / 66.7% | **15本 / 85.9%** | 82.4% |
| 堅い・厚め | 115本 / 68.8% | **113本 / 85.1%** | 83.6% |
| 複勝・単勝 | 13本 / 93.3% | 21本 / 90.3% | 99.3% |

- **honmei は設計どおり**: 9/14以降ずっと1日5本ちょうど。後半は 85.9%（n=15）。
  **的中時の最小倍率が ×0.93 → ×1.35 に改善し、元本割れが消えた**（後半8本の実倍率
  1.35/1.35/1.38/1.42/1.48/1.73/2.54/2.87）。ただし ×1.62 の保証未達は 5/8 残る。
  **深い外れが消えたのが時刻変更の効果**で、浅い未達は倍率の問題。
- **穴モードの発火が激減**: 9/13 53本 → 9/16以降 3・5・10本（設計は1日14本）。
  9/14 の18本から既に落ちているので時刻変更だけが原因ではない。**CSVに skip_reason が無く診断できなかった。**
- 複勝は発火が設計（10.6本/日）の半分以下で、的中30/34（88.2%）。的中時の払戻は100円が10本＝元返し。
- 7日・4モード合算（仮想）: 投資 1,024,600円 / 払戻 770,560円 / **回収率 75.2%**。

### CSV書き出しに診断用の列を追加（`load_scored`）
このレビューで足りなかった列を入れた: **`skip_reason`**（見送り理由）、
**`minutes_before`**（`post_time_at_pred − created_at` の分。**9/16の時刻変更の前後を
CSVだけで判別できる**）、`post_time_at_pred`、`tags` / `solid_tags`（穴の前向き検証用）、
`odds_estimated`、`race_type`。39列になった。

## 飛躍17（2026-09-18、`reports/research/leaps7.md`）— **2026年だけの否定的結論は検出力不足だった**

**動機**: `lane1_place.md` で、`stadium_study.md` の「場の差は無い」が2026年のみ・1場257Rという
検出力不足による誤りだと分かった。**同じ理由で切り捨てた区分を46万レースで測り直す。**

### 本物だったもの: **SG・G1 の予選ラウンド（1〜11R）で 1-2-3 → 93.79%**
n=24,874（1日7.9本）、探索(2018〜23) 93.2% → 確認(2024〜26) 95.1%。
**`combo123.md` の最高 86.0% を7.8pt上回り、本プロジェクト最良の3連単ルール。**

- **交絡ではない。むしろ逆**: SG/G1 は **1〜11R で高く（SG 95.5%・G1 93.4%）、12R優勝戦では低い**
  （SG 74.2%・G1 81.7%）。「大会が大きいから堅い」ではなく「**予選では最強の選手が1号艇に入る**」
  という番組の構造。優勝戦は6人とも一流なので当然competitiveになる。**機構の説明が付いた。**
- **市場は「出やすさ」を値段に入れていない**: 1-2-3 の出現率は SG ×1.14・G1 ×1.12 なのに、
  的中時の平均配当は ×1.02・×1.01 しか下がらない（積 1.163 / 1.133。1.00なら織り込み済み）。
- **層を固定した並べ替え検定**（場の平均を引き、場の中でグレードを入れ替え200回）で
  **G1 は 0/200**、SG 4/200、G3 4/200。

### 確定できないもの: SG/G1 1〜11R × 勝率差0〜1 = **107.68%**
n=4,162、探索105.9% → 確認111.8%、帰無2000回で 9/2000。**それでも採用しない。**
1. **多重検定**: 曜日7・時間帯5・レース番号12・節の日数6・グレード5・重ねた条件4 の**約39通り**の最大値。
   素のp値0.0045 × 39 = **0.18** で補正後は有意でない。
2. 同じ帰無で **100%超えは 227/2000（11.3%）** 出る。n=4,162 では偶然でも起きる。
3. 95%区間の下限を100%より上げるには、真値107.7%のままでも **18年**（1日1.3本）。
   `calibration` の「crowd_bias 0.2%尾は99.5年」と同型で**原理的に確認できない**。

### 採用しないもの
- **曜日**: 場・グレード・節の日数を揃えた残差で 月+4.80pt / 金−3.20pt。層内並べ替えで最大値 7/200 と**境界**。
  機構が思い当たらないので不採用。
- レース番号・時間帯・節の日数: 探索確認で符号は揃うが帰無との差が小さく、機構も無い。

### 方法論の教訓（重要）
**「帰無と区別できない」は「差が無い」ではなく「この標本では見えない」だった。**
`stadium_study.md`（場）に続いて2例目。**2026年のみで出した否定的結論は全部やり直す価値がある。**
ただし 1-2-3 は的中7%・配当の裾が重いので、46万レースでも95%区間は ±1.1pt までしか縮まない
（±0.6pt を期待したが届かなかった）。**標本を13倍にしても精度は3.6倍にしかならない。**
