# boatlab — 引き継ぎメモ（Claude Code 用）

競艇3連単の「研究・予想システム」。当てる予想を出すことではなく、過去データで検証しながら
予想モデルを作り・改善し続けることが目的。開発の経緯と設計は `docs/00`〜`docs/10` を参照。

## 絶対に守るルール（リーク防止）
- レース結果を予想生成に一切使わない。予想は結果取得より前に保存する。
- 過去の予想は書き換えない（`predictions` テーブルは追記専用。トリガで UPDATE/DELETE を拒否）。
- 特徴量は対象レースの前日までの実績だけを使う（`boatlab/features/asof.py` の as-of 原則）。
- バックテストの封印テスト期間はモデル版ごとに1回だけ評価する。
- 見送りレースも仮想採点して記録する。モデル・設定はバージョン管理する。
- 正直さを最優先。「当たるように見せる」ことはしない。効かなければ効かないと報告する。

## いまの状態（2026-09-04 時点）
- 本番: KAGOYA VPS で稼働（PWA + スケジューラ）。`deploy/`、手順は `docs/10`。更新は VPS で
  `sudo bash /opt/boatlab/deploy/update.sh`。
- 採用モデル: **Model 1.0**（active）。Model 1.1（3seedアンサンブル）は shadow 並走候補で未採用。
- 買い方: **絞り込み型**（role='focused'、`FocusedParams`、docs/04 §16）を15点固定と並行して毎日保存。PWA上部トグルで切替。
- 資金配分（15点固定側）: **確率比例1乗**（`settings_versions.extra.staking`）。均等と回収率は同等だが「的中しても
  赤字」の割合が下がるため採用。`boatlab/model/staking.py`。
- これまでの正直な結論: 現行モデルに市場優位はない（3連単 log-loss 市場3.70 vs モデル3.78）。
  控除率は実測25%。回収率100%超えの買い方は見つかっていない。詳細は `reports/backtest/`。

## 進行中の作業: fs2 特徴量の効果検証
- 新特徴量42個を7グループに分けて追加済み（`boatlab/features/build.py` の `FEATURE_GROUPS`）:
  entry(進入) / st(実ST) / kimarite(決まり手) / weather(場×コース×風) / form(直近フォーム) /
  exh_trust(展示信頼度) / parts(部品交換)。リーク検査済み。
- 検証: `scripts/run_fs2_experiments.py`。基準値→各グループ単独→効いたものの組合せ、を四半期WFで比較。
  1実験=1サブプロセス（6GBメモリ対策）、期ごとに `.ckpt.pkl` へチェックポイント（途中落ち再開可）。
- 結果は `reports/backtest/fs2_experiments.{csv,md}` に追記。判定は 3連単 log-loss が基準比 −0.001 以上。
- 結果（run_fs2_fast.py、単一分割）: form −0.0058 / exh_trust −0.0014 が有効、form+exh_trust=3.8090（base 3.8171）。他は誤差。
- Model 1.2（form+exh_trust）本検証済み（`scripts/run_model12_wf.py`、`reports/backtest/model12_{valid,test}.md`）:
  valid log-loss 3.8108→3.8039、封印テスト 3.7983→3.7894。精度は一貫して改善、回収率は横ばい
  （15点 78.9→79.1%、絞り込み型 81.4→80.9%）。**未採用・未並走**（ユーザー判断: 別の改善を先に）。
  probstore は `data/probstores/m12_{valid,test}.pkl`（サンドボックスのみ）。
- 市場一致フィルタ検証（`scripts/run_market_filter.py`、`reports/backtest/market_filter_test_lgb.md`、2026実オッズ）:
  P/Q上限・β縮約・1位一致ゲートは再現せず不採用。安定して効いたのは上限オッズ50→20倍と期待値1.0→0.8
  （的中13%→23%、回収率83%で同等）。設定タブで変更可能（コード変更不要）。実オッズ期間の現行設定は的中≈13%・回収率77〜82%。

## データの置き場所（重要）
- `data/`（lab.db 2.2GB、features 1.3GB、probstores 3.2GB、models）は **.gitignore でリポジトリに入らない**。
- 別マシンで動かすときは lab.db を作り直す必要がある: `lab init-db` → `lab ingest-history --from 2018-01-01
  --to <今日>`（数時間）。特徴量キャッシュは `build_entry_dataset` が初回に自動生成する（1〜2時間）。
- VPS には lab.db がある（構築時に生成済み）。fs2 実験用の probstores はクラウドのサンドボックスにのみ存在。

## よく使うコマンド
```
pip install -e .                         # 開発インストール
python -m pytest tests -q                # テスト（DBが要るものは data/lab.db が必要）
lab init-db                              # テーブル+トリガ作成
lab ingest-history --from 2018-01-01 --to 2026-08-30
lab train --version 1.2 --seeds 7,17,27 # モデル学習→candidate登録
lab predict --version 1.0 --stage final # 締切前レースに予想保存（追記専用）
lab score                               # 結果が出た予想を採点
python scripts/run_backtest_campaign.py # 検証キャンペーン一式
python scripts/run_fs2_experiments.py   # fs2 グループ効果検証（進行中）
```

## コード地図
- `boatlab/ingest/` 取得層（Open API v3 / turnmark / 公式odds3t、原本キャッシュ、レート制限）
- `boatlab/store/` SQLite スキーマ・追記専用トリガ・書き込み
- `boatlab/features/` as-of 特徴量（asof.py が要）、fs2 拡張、履歴読込
- `boatlab/model/` strength(LightGBM) / trifecta(PL) / calibration / market / selection(15点) / staking / pipeline
- `boatlab/backtest/` dataset / walkforward(ProbStore) / metrics
- `boatlab/ops/` daily(ingest/predict/score/train) / scheduler(日次ループ)
- `boatlab/api/app.py` FastAPI + PWA(`web/`)
- `scripts/` バックテスト・実験の実行スクリプト

## 優先指標の順（設計方針）
回収率 ＞ 損益 ＞ 的中率 ＞ 期待値 ＞ 最大DD ＞ 最大連敗 ＞ 穴回収率 ＞ 直近成績。
実戦投入条件の具体値は後で決める（設定で変更可）。自動購入はしない。最終判断はユーザー。

## 買い方の自動研究（2026-09-08 追加）
- `boatlab/research/strategy.py`: 固定グリッド36通り（期待値×オッズ上限×自信×点数）を、研究用データ
  `research_data/bt2026_top20.npz`（2026年実オッズ3.7万R、Model 1.0 の上位20点確率、git管理・3MB）と
  本番の確定予想（predictions.probs/odds_used × results）に当てて評価。探索(1〜5月)/確認(6〜8月)/実運用の3期間＋
  順送り検証（各月、その月より前の回収率95%区間下限が最大の候補を当てる）。出力 `data/research/strategy_research.json`。
- スケジューラ 02:45 に `research` ジョブ。API `/api/research`（GET）・`/api/research/run`（POST、別スレッド）。
  PWA モデルタブ「買い方の研究（自動）」。**設定は自動で変えない**（候補の印付けまで。採用はユーザー）。
- 実測（バックテストのみ）: 順送り 81.3%。グリッドは増やさない（多重検定対策）。

## 場（レース場）別の検証（2026-09-08、結論: 効果なし）
- `scripts/run_stadium_study.py` → `reports/backtest/stadium_study.{csv,md}`。2026年実オッズ・現在設定。
- 全期間（1〜8月）で回収率100%超は **びわこ 102%（n=257、95%区間 81〜124%）のみ**。区間下限が100%超の場は0。
- 探索(1〜5月)の場別順位は確認(6〜8月)に引き継がれない: 順位相関 ρ=−0.03（p=0.91）。
- 場ラベルを無作為化した対照でも「最高の場」は平均108%、100%超が平均2.2場出る（実データは111%・3場）＝区別できない。
- 探索上位3場だけ買う → 確認 88.7%（無作為3場の分布で上位21%相当。全場82.6%との差は誤差の範囲）。
- 場ごとに36候補から最適パラメータを選ぶ → 確認 80.1% で**全場一律より悪化**（場あたりのデータが少なく過学習）。
- 「荒れやすさ（平均払戻）」と回収率の相関は 0.25（n=24、有意でない）。場別の分割はしない方針。

## 券種別の再検証（2026-09-09、実払戻のみ。結論: どの券種も100%未満）
- `scripts/run_bettype_study.py` → `reports/backtest/bettype_study.{csv,md}`。3連単120通り確率を各券種に
  周辺化し、results.payouts（公式の確定配当）で採点。**オッズ推定を一切使わない**（09-03 の検証は推定オッズ）。
  探索 2024-01〜2025-06（80,628R）／封印テスト 2025-07〜2026-08（62,801R）。
- 確率上位k点・100円均等の封印テスト回収率: 複勝1点 94.8% / 単勝1点 92.7% / 2連複2点 85.9% /
  ワイド1点 85.3% / 3連単1点 82.0% / 2連単2点 82.0% / 3連複1点 81.0%。点数を増やすほど悪化。
- 自信上位1%のレースに絞ると 複勝 98.7%（区間97〜101%、629R）・単勝 97.9%（95〜101%）が最接近。100%超えなし。
- モデルは公式払戻率を上回る＝素の市場平均には勝っている（3連単で+7pt）が、控除率25%を埋める25ptには届かない。
- **訂正**: 競艇の払戻率は全券種一律75%（控除率25%）。競馬と違い単勝・複勝も80%ではない
  （実測 Σ(1/確定オッズ) の逆数＝単勝73.6%・3連単74.8%）。単複が有利に見えるのは控除率ではなく分散の小ささによる。

## 単勝プールの歪み（2026-09-09、**初めて100%を超えた**。未検証・要追加データ）
- `scripts/run_pool_arbitrage.py` → `reports/backtest/pool_arbitrage.md`（commit 370aca8〜）。2026年1〜8月・確定オッズ・実払戻。
- 「1着になる確率」の精度（対数損失・確認期間）: **3連単プール 1.1474 < モデル 1.1720 < 単勝プール 1.2405**。
  市場（3連単プール）はモデルより正確。歪んでいるのは**薄い単勝プール**の方（売上シェア約0.08%）。
- 買い方: 単勝20倍以下で「3連単オッズ由来の勝率 ≥ 単勝オッズ由来の勝率×2」の艇を100円。
  **8か月4,571本で回収率184.1%**、月別160〜205%で全月プラス、上位10本の払戻を除いても179.7%。モデルは不要。
- 資金規模の制約が本丸: 単勝プール32,000円想定で1点100円→178.6%、1,000円→139.0%、2,000円→114.4%、5,000円→79.6%。
  1点1,000〜2,000円が上限。買いは全レースの約11%（1日19本）。
- **未検証**: 使ったのは両プールとも確定オッズ。実際は締切前オッズしか使えない。次にやること＝
  締切前の単勝オッズ（と3連単オッズ）の定期取得を本番に追加し、締切前→確定の動きと歪みの残存を実測する。
- 自動購入はしない・最終判断はユーザー、の原則は変わらない。

## 単勝・複勝プールの歪み: 観測フェーズの実装（2026-09-09）
- 目的は「締切前オッズでも歪みが見えるか」の実測。**購入はしない・設定も自動で変えない**。
- 取得: 公式 `oddstf`（単勝＋複勝を1回で）を締切6〜12分前と2〜4分前の2回。3連単は従来どおり6〜12分前に1回。
  公式サイトの上限は3秒間隔・1日1,000回、レースは平均159・最大216なので合計480〜650回に収まる。
  設定タブで停止すると取得ごと行わない（`extra.poolgap.enabled`）。
- 判定ロジック `boatlab/research/poolgap.py`: 3連単オッズ由来の確率 ÷ そのプールのオッズ由来の確率 ≥ 閾値
  （単勝2.0・複勝1.5、オッズ1.1〜20倍）。複勝は下限側オッズを使うので判定は厳しめ。
- 記録は `pool_gap_picks`（**追記専用トリガ付き**）。採点は読み取り時に results.payouts と突き合わせる方式で、
  テーブルを書き換えない。API `GET /api/poolgap`、PWA モデルタブ上部に表示。
- **合否の基準**（先に決めた）: ①締切前オッズと確定オッズの差が±10%以内の割合が過半（n≥200）
  ②締切前オッズで選んだ候補の仮想回収率が120%以上かつ95%区間下限が100%超。外したら打ち切る。
- パーサ未検証: サンドボックスから boatrace.jp に到達できないため `parse_oddstf` は実ページで未確認。
  本番で `lab check-oddstf --stadium <場> --race <R>` を1回実行して確認すること。生HTMLは
  `data/raw/official_web/oddstf/` に残る。読めなければ空 dict を返して記録をスキップする（既存処理は無影響）。

## 画面で自己診断できるようにする（2026-09-11）
- 発端: レース一覧が「5/12R終了・次 12:44締切」で止まって見え、上流待ちなのか故障なのか画面から判別できなかった。
  実体は上流 today.json が `result` の枠だけ作って中身（着順・払戻）を入れていない状態で、こちらは
  `_add_result` の pending_ok 分岐で意図的にスキップしていた＝正常動作。翌朝06:10の turnmark 取込で埋まる。
- `GET /api/status`: 本日の結果（DB件数・結果待ち件数・**上流に中身のある結果が何件あるか**を突き合わせ、
  ok / upstream（上流待ち）/ ng（取り込み異常）/ unknown を判定）、公式オッズ件数（3連単・単勝・複勝）、
  プール差の記録件数、確定予想と推定オッズ混入件数、直近ジョブ、24時間の取得失敗。上流照会は120秒キャッシュ。
- PWA 設定タブ最上部に「システムの状態」。レース一覧は締切超過で結果の無いレースを**結果待ち**表示に
  （場カードに「結果待ち◯R」、レース行にピル）。`_freshness` は2時間以上前の未完了ジョブを running から
  stuck に分離（過去の異常終了がいつまでも「実行中」に見えるのを防ぐ）。
- `lab today-status`（CLI）でも同じ突き合わせができる。tests/test_status_api.py で4つの判定を固定。

## 締切前オッズの動き（2026-09-12、`lab odds-drift`）
- 実測（9/11、144R・16,212買い目、Σ(1/オッズ)は締切前1.333/確定1.337で健全）: 全体の中央値 +34.5%、
  ±10%以内は14.4%。帯別は 5〜10倍 −8.1%、10〜20倍 −8.7%、20〜50倍 −2.3%、50〜200倍 +22.4%、200倍〜 +71.1%。
- **含意**: 単勝プール（中央値−50%）ほどではないが3連単も安定ではない。**これまでの実オッズ検証は
  すべて確定オッズで選定しており、絞り込み型の83%も同じ先読みバイアスを含む**（買う帯は中央値−8.4%なので
  概ね0.92倍に割り引いて読む）。実測値は9/11以降の本番データで直接確認する。

## route A「市場と組み合わせる」の結論（2026-09-12、**打ち切り**）
- `scripts/run_market_combine.py` → `reports/backtest/market_combine.md`。Benter 1994 の形
  `p ∝ モデル^a × 市場^b` を探索1〜5月で最尤推定し、確認6〜8月で1回評価（36,944R、3連単確定オッズ）。
- 合成は市場を上回った: 3連単 対数損失 市場3.7077 → 合成3.7026（**−0.0051**、a=0.150 b=0.926）。
  1着確率でも 1.1565 → 1.1552（−0.0014）。ただし勝ったのは**僅差**。
- **ケリー基準で決着**: 全通り比例買いの期待対数成長 = 情報優位 + log(1−控除率) = +0.0051 − 0.2877 = **−0.2826**。
  必要な優位の **56分の1** しかない。
- 実測でも、確認期間 1,701,240 買い目のうち「合成確率÷市場確率 > 1.333（＝期待値1.0）」は **12件**だけ（最大1.384）。
  モデル単独なら26.8%が期待値1.0超えに見えるが実回収率69.8%＝winner's curse。合成が a≈0.15 まで縮めるのが正しい判断。
- **一般的な結論**: 「この買い方が駄目」ではなく「このモデルと市場の差では、どんな買い方でも駄目」。
  勝つにはモデルを市場より大きく正確にするしかなく、必要な差は fs2 全体の改善（0.008 nats）の数十倍。

## 回収率の式が閉じた（2026-09-12、`scripts/run_sweet_spot.py`）
- 全券種で「市場（3連単オッズ）がいちばん確からしいと見る1点」を買い、確信の高いレースに絞って実測。
- **元返し（払戻100円の下限）は天井ではなく補助金**だった。元返しの割合が高い券種ほど回収率が高い
  （複勝 元返し77.7%→99.1% ＞ ワイド22.9%→91.2% ＞ 2連複1.1%→86.0% ＞ 3連単0%→83.1%）。
  確率0.94の艇の理論払戻は約80円だが下限で100円もらえる＝25%を胴元が被っている。
- **回収率 = 的中率 × max(1.00, 0.75 ÷ 市場確率)**。
  下限が効く側では回収率＝的中率（<1、実測の最高は94.6%）なので**絶対に100%を超えない**。
  下限の外では 的中率÷市場確率 > 1.333 が必要で、合成モデルでも170万買い目中12件しか出ない。
- 複勝上位10%の99.1%は両領域の混合。**券種の選び方では壁を越えられない**ことが確定。

## 群衆の癖を直接測る（2026-09-12、`scripts/run_crowd_bias.py`、結論: 実在するが1.14で足りない）
- 同じ特徴量で条件付きロジットを2本: A＝実際の1着を当てる / B＝市場の1着確率を再現する。
  係数の差 β_B−β_A が群衆の癖。線形・十数パラメータなので過学習の余地が小さい。
- **艇番が最大のズレ**: 2〜6号艇の係数が実際より大きい＝1号艇を過小評価（6号艇+0.228が最大）。
  これが人気薄バイアスの正体で、複勝99.1%を支えているのもこれ。
- **過大評価**: 展示タイム +0.120、級別 +0.084（目に見える情報に反応しすぎる）。
  **過小評価**: 全国勝率 −0.068、展示ST −0.055、モーター2連率 −0.051（地味な数字を軽視）。
- ズレの方向に賭けると 実勝率÷市場確率 は 上位10%で1.072 → 1%で1.139 → 0.2%(n=70)で1.243 と
  単調に上がるが鈍化し、**必要な1.333に届かない**。艇番を除くと上位2%で1.135が限界。
- 線形モデルA単独の1着対数損失は 1.1981 で市場 1.1455 に負ける（総合点では勝てない）。

## 他競技（競輪・競馬）で回収率100%は超えられるか（2026-09-12 調査、`reports/research/other_sports.md`）

競艇で導いた式 `回収率 = 的中率 × max(1.00, 払戻率 ÷ 市場確率)` はそのまま他競技に使える。
違うのは控除率・市場の歪みの大きさ・キャリーオーバーの有無の3点だけ。

- **競輪は一律25%控除（券種差なし）＝競艇と完全に同条件。** 必要比 1.333。構造的な得はゼロ。
- **JRA/地方の単勝・複勝のみ控除20%** → 必要比 1.250。競艇比で必要な歪みが 6.3% 小さい。
  それ以外（馬単・3連複 1.333／3連単 1.379／WIN5 1.429）は競艇と同等か不利。
- 本ツールで実測した競艇の歪みは 頑健 1.14 / テール70本 1.24。1.250 にわずかに届かない。
  → 控除率の差だけでは足りず、競馬の歪みが競艇より大きいことが別途必要。
  北米30場の実測（Green et al.）では本命端でも控除17%に対し回収は約2ポイントのみで、楽観は禁物。
- **キャリーオーバーは控除率をマイナスにできる唯一の仕組み**（WIN5 / 競輪Dokanto! / LOTO。競艇には無い）。
  `還元率 = 払戻率 + CO額 ÷ 売上`。WIN5で100%超の条件は **CO ≥ 売上の30%** で、15年で3回のみ
  （実測期待値 1.039/1.052/1.058）。直近5回は 78〜87%。COが大きいほど売上も増えて薄まる。
  かつ全網羅はパリミュチュエル山分けで成立せず（WIN5全網羅1.89億円、Dokanto!7で9.56億円）。
  **期待値プラス ≠ 実行可能。**
- **実在証明はJRAにある**（裁判で購入額・払戻額が確定）:
  2015年最高裁（大阪）2005-09年 購入28.7億/払戻30.1億＝**104.4%**、ソフト網羅購入。
  2017年最高裁（北海道）2005-10年 毎年100%超、2009年は全3,453R中2,445R(70.8%)で購入。
  → 可能だが**エッジは4〜8%**、5〜6年、年数億円の回転が前提。「良い予想」ではなく「小エッジ×巨大回転」。
- **税**: 払戻は原則一時所得で外れ券は経費にならない。上記2件が雑所得と認められたのは
  継続的・網羅的・機械的という特殊な態様ゆえ。一時所得扱いなら総回収率104%でも税引後マイナスになりうる。

**結論**: 競技を変えること自体は救済策にならない。検証するなら JRA 単勝・複勝で、競艇と同じ手順
（探索/確認分離・ラベルシャッフル・事前登録基準）で `p/q` 上位テールが out-of-sample で 1.250 を
超えるかを測る。作り直しではなく測定の問題。

## 校正（キャリブレーション）で回収率100%を超えられるか（2026-09-12、結論: 超えられない）

**回収率は我々が名乗る確率に一切依存しない。** 決まるのは「どの買い目を買ったか」と「市場がいくら払うか」
の2つだけ。校正が回収率に効く経路は「選択が変わる」「賭け金が変わる」の2つしかない。

- **選択**: 選択の曲線（絞り込み度 × 回収率）は `reports/backtest/{sweet_spot,favorite_edge,crowd_bias}.md`
  で全券種・全絞り込み度について既に測ってある。校正は**この曲線上のどこに立つかを正しくするだけで、
  曲線そのものを持ち上げない**。曲線の最高点は **複勝・市場確信度上位10% の 99.1%**（n=1,435）。
- **賭け金**: 曲線上のすべての点が100%未満なので、ケリー基準の最適賭け金は0。配分では救えない。

### データを貯めることで何が決着するか（本ツールで計算）

| 買い方 | 現n | 回収率 | 1本SD | 現95%幅 | 本/日 | ±0.5%に必要なn / 年数 |
|---|---:|---:|---:|---:|---:|---|
| 複勝 上位10% | 1,435 | 99.1% | 0.255 | ±1.3pt | 15.6 | 2,597 / **0.5年** |
| 単勝 上位10% | 1,352 | 94.5% | 0.409 | ±2.2pt | 14.7 | 6,703 / 1.2年 |
| crowd_bias 0.2%尾 | 70 | 93.2% | 0.831 | ±19.5pt | 0.8 | 27,634 / **99.5年** |

- **決着するのは1つだけ**: 複勝上位10%の99.2%が真に99%台なのか、上限の100.4%側なのか。約0.5年で±0.5%になる。
- **1.243に見えた尾（crowd_bias 0.2%）は検証に99.5年**かかる。原理的に確認できない＝使えない。
- 仮に真値が100.5%だったとしても、15.6本/日 × 100円で **年2,847円**、1,000円でも年28,466円。
  かつ競艇の複勝プールは売上の約0.07%しかなく、増額すれば自分でオッズを潰す。**スケールしない。**

### 100%を超えるのに必要な「新情報」の量

無情報（1着6通り一様）の対数損失 ln6 = 1.7918、市場 1.1565 →
**市場が持っている情報量は 0.6353 nats。** 控除率25%のコストは -log(0.75) = 0.2877 nats。

→ **控除率を埋めるには、市場の全情報量の 45.3% に相当する私的情報が追加で必要**（3連単基準でも26.6%）。
我々のモデル＋市場の合成が市場を上回った分は 0.0051 nats ＝ 市場の情報量の 0.80%、必要量の **1.77%**。

校正はこの0.0051を1ミリも増やさない（校正は情報を追加しない変換）。**足りないのは情報であって校正ではない。**

### 「勝てるレースを選ぶ」という設計思想について
方針として正しく、既に実行済み。全レース購入の回収率75%（＝控除率そのまま）を、選択によって
**99.1%** まで持ち上げてある。残り0.9%は元返し（100円下限）による構造的な天井で、絞り込みを
強めると的中率は上がるが平均払戻が100円に張り付くため積が下がる（上位10%が頂点、上位1%で96.4%）。

## 目的1の総括: 市場オフセット型ロジットによる5族の検証（2026-09-12、`reports/research/goal1_summary.md`）

**測り方を統一した。** 市場確率 q をオフセットに置いた条件付きロジット
`score_ij = log q_ij + γ·z_ij`。γ=0 なら市場が既に持っている情報。市場を作り直す必要がなく
「市場を超えた分」だけを直接測れる。共通土台は `boatlab/research/market_offset.py`。
**以降の目的1の検証はすべてこの形に統一する。**

**プラセボは必ず帰無仮説「市場確率が真」から生成（y ~ Multinomial(q)）。**
1着をシャッフルする作りは誤り。艇番の周辺分布と選択集合の対応が壊れ、選ばれた艇の多くが
1号艇なので比が2.5前後まで跳ね上がる（`wind_forecast.md` §4 に実例）。

| 市場に足した情報 | 確認対数損失 | 市場との差 | p/q上位2% | 必要量比 |
|---|---:|---:|---:|---:|
| 市場のみ | 1.1455 | +0.0000 | 1.000 | — |
| 艇番バイアス | 1.1444 | −0.0010 | 0.935 | 0.35% |
| ＋公表モーター2連率 | 1.1443 | −0.0012 | 1.052 | 0.42% |
| ＋分離推定モーター効果 | 1.1453 | −0.0002 | 1.029 | 0.07% |
| ＋本番風の予測残差 | 1.1444 | −0.0011 | 1.009 | 0.38% |
| ＋今節成績 | 1.1445 | −0.0009 | 0.999 | 0.31% |

プラセボの上位2%比はいずれも0.99〜1.02。実測0.94〜1.05は**帰無と区別できない**。
必要量 0.2877 nats に対し最良で 0.42%。**5族が独立に 0.001 nats 前後へ収束した。**

**診断: 我々のデータはほぼ市場の情報の部分集合。** 公開出走表から計算できるものは全部値段に
入っている。モデルの作り方・目的関数・校正ではこの構造は動かない。動かせるのはデータだけ。

個別の打ち切り記録:
- **アイデアD（風）**: `preview` 気象は1つ前のレースの観測値（一致率99.9%）で、本番風との一致は
  47%のみ。本番風は締切前情報で予測できた（確認期間 RMSE 1.1135→1.0113、+9.2% OOS）が、
  市場を条件づけた後の価値は +0.0000 nats。**予測できることと値段に対して価値があることは別。**
- **アイデアE（モーター分離）**: 公表2連率を条件づけた上積みは無し（M3 1.1453 > M2 1.1443）。
  当初の実装は探索期間の残差から e_m を作りその同じ期間で縮小定数を選んでいた＝答えの漏れ。
  k=10が選ばれ確認期間で市場より悪化(1.1779)。交差適合で修正後は符号もcrowd_biasと整合。
- **アイデアH（今節成績）**: 符号は事前予想どおり市場の過剰反応（今節平均着順 −0.0471、
  1着率 −0.0326）だったが、上積みは −0.0001 nats。

**手持ちデータの外に残るのは展示航走の映像解析のみ**（ターンの安定性・艇の挙動・水面の乱れ）。
出走表に存在しない量で、専門家も定性的にしか扱っていない。ただし映像はDBに無く2コアVPSでは
常時処理が不可能で、標本検証止まり。成功見積もり: 手持ちデータ内ほぼゼロ、映像込みで数%。

## 全レース穴買い（オッズ上限なし）の実測（2026-09-12、`reports/backtest/longshot.md`）

本命端の鏡像。36,251R全数、選定にパラメータを使わないので探索/確認の分割は不要。
`歪みの比 = 回収率 ÷ 0.75 = 実的中率 ÷ 市場確率`（1.00 なら市場は正しい）。

**いちばん人気がない1点を全レース**: 単勝 **34.1%**（比0.739）／複勝 45.9%／2連単 19.5%（比0.542）／
2連複 30.4%／ワイド 45.3%／3連複 27.5%／3連単 **27.6%**（的中8本/36,251R、最高払戻197,220円）。

**単勝の人気順の曲線が教科書どおり単調**（favorite-longshot bias の最もきれいな実測）:
人気1番目 92.2%（比1.042）→ 2番目 79.7%（0.966）→ 3番目 75.6%（0.961）→ 4番目 67.2%（0.938）
→ 5番目 55.2%（0.900）→ 6番目 34.1%（0.739）。
3連単も人気1〜5番目で比 1.047〜1.085（**全研究中で最良の比 1.085 が人気2番目**）、ROI 77〜80%。

**「穴が過大評価なら逆で儲かるのでは」への答え: 逆を取る取引が存在しない。**
人気薄をk点外して残りを払戻均等で買うと 86.5%（外さない）→87.1→88.1→89.1→90.2→92.2%（人気1点）と
単調に上がるが、これは「本命に寄る」のと同じで単勝本命端に一致するだけ。100%超えはどこにも無い。
最不人気を空売りできれば100円につき約45円だが公営競技に売り側は無い。
そして穴の誤りがプール全体に対して持つ大きさは **0.65%**（1.59% × (75%−34.1%)）＝配っても+0.7pt。
**市場のいちばん大きな誤りは、いちばん小さな確率の上に乗っている。だから取り出せない。**

注意: この表は賭け金に3連単プールの確率、払戻に単勝の確定配当を使うのでプール間の値付け差
（単勝1.2405 / 3連単1.1474）が一部混ざる。その差は締切前には取れない（観測フェーズで中央値−50%）。

## 万舟券の発生と特徴（2026-09-12、`reports/backtest/manshu.md`）

万舟＝3連単配当1万円以上＝オッズ100倍＝市場確率 0.0075。
**市場が示す万舟確率 = 確定オッズ100倍以上の買い目の確率の合計** で直接計算できる。

**発生頻度**（36,251R・241日）: 万舟 5,982本＝**16.50%＝1日24.8本＝6レースに1回**。
5万円以上 709本（1日2.9本）／10万円以上 208本（0.9本）／20万円以上 28本（0.1本）。
最高 501,250円、平均 7,076円、中央値 2,490円。月別の変動は 15.6〜17.2% で安定。

**市場は万舟を17%過大評価している**: 実測 16.50% / 市場が示す値 19.23% → 比 **0.858**。
`longshot.md` の穴買い（単勝34.1%）と同じ向き。

**過大評価は「市場が堅いと見たレース」でいちばん大きい**（市場19.23%を10分割した校正表）:
市場12.6%→実測9.3%（比0.736）… 市場27.4%→実測27.5%（比1.006）。単調。
→ **群衆は「堅いレースにも万が一がある」を買いすぎている。** 荒れると見たレースの値付けは正確。
市場の最高1着確率が70%以上のレースでも万舟率は12.4%あるが、市場はそれを16.0%と見ている（比0.774）。

**万舟が出やすい条件は実在する**: 波5cm以上18.4%／風7m以上18.1%／展示で2人以上が前づけ19.5%／
節の初日〜2日目18.2%／G2 20.2%・SG 19.6%・一般16.2%／場は江戸川19.6%・三国18.9% ⇔ 福岡12.1%・尼崎14.1%。
**ただしどの区分でも「実測÷市場」は1.00未満で、市場はこれらを全部知っている。**

**市場を超えた予測力は +0.0023 nats**（市場が示す万舟確率をオフセットに置いたロジスティック回帰、
確認期間の二値対数損失 0.4441→0.4418）。これは本研究で測った中では最大だが、必要な 0.2877 nats の 0.8%。
分散（荒れ）の予測は平均（勝者）の予測より少しだけ簡単だが、分散に払う券種は存在しない。

**目的2への応用は不発**: 複勝（市場確信度上位10%、99.1%）に荒れ信号 γ·z での追加見送りを入れても
99.0〜99.3%で動かない。天井は元返し（100円下限）で決まっており荒れの読みでは動かせない。

**万舟の正体は決まり手が一言で表す: 逃げ 3.6% / 差し 27.0% / まくり 33.2% / まくり差し 36.5%。**
万舟とは「1号艇が逃げられなかったレース」。1号艇の1着率約55%を市場は正確に値付けしている。

## 2モード表示ツールの設計材料（2026-09-12、`reports/backtest/two_modes.md`）

**穴モードの最重要の設計判断: 「穴」＝最不人気ではない。** 3連単を人気帯で買った実測（全36,251R）:

| 買う帯 | 点数 | 的中率 | 平均払戻 | 最長連敗 | 回収率 |
|---|---:|---:|---:|---:|---:|
| 人気1〜5 | 5 | 37.4% | 1,042円 | 23R | 78.0% |
| 人気16〜30 | 15 | 16.5% | 6,519円 | 67R | 71.6% |
| **人気20〜40** | 21 | 16.2% | 8,916円 | 51R | **68.8%** |
| 人気30〜50 | 21 | 10.3% | 13,517円 | 79R | 66.3% |
| 人気50〜70 | 21 | 5.2% | 25,484円 | 143R | 62.7% |
| 人気110〜120 | 11 | 0.3% | 138,484円 | **2,315R** | **37.5%** |

→ **万舟級の配当を狙うなら人気20〜50帯。最不人気1点(27.6%)の2.4倍の回収率。**

**レース選択が穴モードでは効く**（的中率モードと違って）: 市場が示す万舟確率の高い順に絞ると
人気20〜40の回収率は 68.8% →（上位30%）74.9% →（上位10%）**80.5%**。
歪みの比で 0.917 → 1.073。選定がオッズの**水準**でなく**散らばりの形**なので、
`pool_arbitrage.md` で崩れた選定とは性質が違う（要・締切前オッズでの確認）。

**現実的な運用設定の実測**:

| モード | 設定 | 1日の投資 | 的中率 | 月あたり的中 | 最長連敗 | 回収率 | 月の期待損失 |
|---|---|---:|---:|---:|---:|---:|---:|
| 穴 | 1日5R × 人気20〜40（21点） | 10,500円 | 22.5% | 34本 | 30R | 79.8% | 約63,600円 |
| 穴 | 1日5R × 人気20〜30（11点） | 5,500円 | 13.2% | 20本 | 40R | 78.2% | 約36,000円 |
| 的中率 | 1日5R × 複勝1点 | 500円 | 95.8% | — | 2R | **98.9%** | 約165円 |
| 的中率 | 1日15R × 複勝1点 | 1,500円 | 93.7% | — | 3R | 98.0% | 約900円 |

**月次収支は全帯・全8か月がマイナス**（全レース購入時）。人気1〜5（回収率78%）ですら 0/8か月。
**穴モードの期待損失は的中率モードの約385倍**。ツールに載せる際は必ず両方の「月の期待損失」を併記する。

※ すべて確定オッズでの測定。締切前オッズでの再現性は9/11から貯めているデータで要確認。

## 条件ベース選定ルールの検証（2026-09-12、`reports/backtest/condition_rules.md`）

**「期待値の高いものを選ぶ」はモデルの期待値では実装しない。** `market_combine.md`: モデルが
期待値1.0超えと見た買い目の実回収率は69.8%。代わりに**市場のオッズだけで決まる箱ごとの実測比**を
較正表にした（縦＝市場が示す万舟確率5分割、横＝候補の市場確率10分割、探索期間で作成）。

**較正表（実勝率÷市場確率）の最大値は 1.152**（必要なのは1.333）。パターンは明快:
候補の市場確率が高いほど比が上がり（0.426→1.152）、**堅いレースほど穴側の比が低い**
（堅い0.426 ⇔ 荒れ0.771）。`manshu.md` の「群衆は堅いレースの万が一を買いすぎ」と同じ絵。

**結論: 較正期待値による候補選定は不発。** 上位k点（k=5/10/20）× 閾値（なし/0.72/0.75）の
9通りすべてで確認期間の回収率は **69.0〜72.1%**、プラセボ（帰無「市場確率が真」）は **75.4〜76.1%**。
**どの設定も帰無を下回る。** 閾値条件を足すとむしろ悪化（条件なし70.4% → 0.75以上69.0%）。

**効くのはレース側の条件だけ**（人気20〜40の21点固定、確認期間で再評価）:
絞らない 71.2%（プラセボ74.9%）→ 市場万舟確率 上位30% **77.7%**（74.8%）→ 上位10% **83.3%**（76.5%）。
上位10%は1日14.0R・29,377円/日・的中23.3%・最長連敗26R。**95%区間 75〜92% で下限が帰無に接する。**
→ **「でたらめに穴を買うよりわずかにマシ」が穴モードの上限。利益が出る設定は存在しない。**

**的中率モードの条件（複勝1点・市場の2着以内確率のみ、確認期間）**:
0.80→57.1R/日・96.6%／0.85→32.9R/日・97.1%／0.88→19.2R/日・98.2%／
**0.90→10.6R/日・99.3%（95%区間98〜101%、最長連敗2R）**／0.92→4.4R/日・97.9%。
→ 閾値 **0.90** が最良。ただし確定オッズ基準で、締切前は中央値で約6.14pt低いので要再較正。

## 3連単（堅い予想）の「当たったのにマイナス」対策（2026-09-12、`reports/backtest/no_hit_loss.md`）

的中でも赤字は 払戻 < 投資合計 のとき。堅いレースほど本命の配当が安いので、購入判定レースでは
**均等配分で60.5%、確率比例2乗でも57.7%が「当たったのに赤字」**（配分では解けない）。

**保証つき配分**で構造的に解く: 各点を `ceil(予算 ÷ オッズ)`（100円単位切り上げ）で買い、
Σ賭け金 ≤ 予算 が成り立つ点数まで**本線を後ろから削る**。すると
**どの点が当たっても払戻 ≥ 予算 ≥ 投資** が成り立つ。

2026年・実オッズ・信頼度0.70以上（購入判定）での実測:

| 方式 | 平均点数 | 1レース投資 | 的中率 | 的中でも赤字 | 回収率 |
|---|---:|---:|---:|---:|---:|
| 10点・100円均等 | 10.0 | 1,000円 | 70.0% | **60.5%** | 79.3% |
| 10点・払戻均等（3,000円） | 10.0 | 3,016円 | 70.0% | 22.4% | 81.0% |
| **保証つき・予算3,000円** | 8.8 | 2,841円 | 66.2% | **1.0%** | 81.4% |
| 保証つき・予算5,000円 | 9.3 | 4,596円 | 68.1% | 1.2% | 81.4% |

→ 的中率 70.0→66.2% と引き換えに、的中でも赤字 60.5%→1.0%（残りは返還・端数）。回収率は不変（配分は選定を変えない）。
**予算3,000円を既定にする（今の1レース予算と同じ）。**

注意: 100円単位の丸めのせいで予算1,000円では払戻均等が成立しない（実質均等になる）。最初の実装は
これで「的中でも赤字25%」が残り、原因に気づいて作り直した。払戻均等には予算2,000円以上が要る。
削る判断に確定オッズを使っているので締切前では点数が前後する（本命は締切にかけて人気が集まり
オッズが下がるため、締切前に成立しても確定で崩れることがある）。

## 3モード表示ツール（2026-09-12 実装、`boatlab/model/modes.py`、modes_version="modes1"）

画面のメインは **穴狙い（3連単）／堅い予想（3連単）／複勝・単勝** の3モード。旧の15点固定・絞り込み型は
「旧モード（比較用）」ボタンから見られ、記録はこれまでどおり裏で続く（300レース到達時のCSVもそのまま）。

| role | 選定 | 発火条件 | 買い目 |
|---|---|---|---|
| `ana` | 市場 | 市場が示す万舟確率 ≥ 0.2495（上位10%） | 3連単 人気20〜40番目 21点 × 100円 |
| `katai` | モデル本線の並び＋保証つき配分 | 信頼度 ≥ 0.70、かつ3点以上成立 | 各点 `ceil(3000÷オッズ)`、Σ ≤ 3,000円になるまで後ろから削る |
| `place` | 市場 | 複勝: 2着以内確率 ≥ 0.90／単勝: 1着確率 ≥ 0.7709 | 各1点 × 100円 |

- **`predictions` に role='ana'/'katai'/'place' として追記**（既存の追記専用トリガ・採点をそのまま使う）。
  確定予想（stage='final'、締切4〜10分前）と同時に `daily._record_modes` が書く。本体・絞り込み型・3モードで
  1レース5行。`flags.params` に使ったしきい値を毎回保存するので、設定変更の前後が記録で追える。
- **市場ベースの2モード（ana/place）は推定オッズでは絶対に動かさない**。公式3連単オッズが無いレースは
  `skip_reason='odds_estimated'` として記録される（黙って欠ける記録は無い）。
- 複勝・単勝の採点は `daily.score_place`（`results.payouts` の place/win）。3連単2モードは既存 `score_race`。
- API: `/api/modes`（3モードの今日＋累計＋実測値＋月の期待損失）、`/api/today?mode=ana|katai|place`。
  画面は `measuredCard` で **実測回収率・的中率・平均払戻・最長連敗・月の期待損失を常時表示**（ユーザー指定）。
- CLI: `lab modes`（今日の記録）、`lab modes --stadium N --race R`（最新の公式オッズで今の選定を計算、記録はしない）。
- **しきい値はすべて確定オッズで決めた値。** 締切前オッズでは市場の確信度が低めに出るので発火は少なめになる。
  記録が2週間ほど貯まったら締切前基準で再較正する（自動では変えない。settings_versions.extra.modes で人が変える）。
- 設計の根拠: `two_modes.md`（帯の選択）、`condition_rules.md`（EV選定は不発・レース条件のみ有効）、
  `no_hit_loss.md`（保証つき配分）、`manshu.md`（万舟確率）。

### 2026-09-12 夕方の修正
- **状態画面の「確定予想 異常: n件が推定オッズ」は誤表示だった。** `flags LIKE '%odds_estimated%'` は
  `"odds_estimated": false` にも一致するので全件が異常に見えた。`json_extract(flags,'$.odds_estimated')=1` に修正。
  実際は「3連単141件」取れており正常。
- 3モードとも**見送りでも「買うならこれ」を記録**する（旧モードと同じ扱い）。見送りレースも仮想採点され、
  条件の良し悪しを後から測れる。推定オッズのときだけ市場ベースの2モードは何も出さない。
- 複勝・単勝の combo は主キー衝突を避けて「複1」「単1」の形。`score_place` は数字だけ取り出す。

### 2026-09-12 夜: 公式3連単オッズの「1000倍以上」が読めていなかった（実オッズ未取得の真因）
- 公式ページは 1000倍以上のオッズを **小数点なしの整数**（"1274"）で表示する。パーサは `\d+\.\d+` しか
  オッズと認めておらず、それらが None になっていた。大村12Rでは120通り中37通り（本命が堅いレースほど多い）。
- 予想側は「数値が100通り以上」で実オッズ採用を決めるので、**堅いレースほど推定オッズに落ちていた**。
  9/11の確認（`check-odds3t`）はキー数しか見ておらず、値が数値かを確認していなかった＝私の確認不足。
- 修正: 2桁以上の整数もオッズとして受ける（1桁は艇番）。カンマも除去。`check-odds3t` は数値の数で判定。
- 診断コマンド: `lab why-estimated --stadium N --race R`（DBから照合をやり直してどの条件で落ちたか）、
  `lab dump-odds3t --stadium N --race R`（保存済み生HTMLを再パースし、読めないセルを生HTMLごと表示）。
  生HTMLは DB ではなく `data/raw/official_web/odds3t/YYYYMMDD/` にある。
- 本番の CLI は `sudo docker compose exec app lab …`。ホストに `lab` は無い。
- 影響: 9/11〜9/12 の確定予想のうち、1000倍超が21通り以上あるレースは推定オッズで作られている。
  記録は追記専用なので直さない（flags.odds_estimated=true で識別できる）。
